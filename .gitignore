.DS_Store
node_modules/
dist/
*.log

# Local Python helper/test bytecode
__pycache__/
*.py[cod]

# Local environment variables (API keys, etc.)
.env
.env.*
frontend/.env
frontend/.env.*

# Allow example env files to be committed
!frontend/.env.example

# Dev server logs
frontend/.logs/

# Local MRI/DICOM sample images (not for source control)
Critical MRI Source Images (LLM Agent - do not delete)/
DCS-volume-copy-*/

# Local test artifacts (downloaded DICOMs, zips, etc.)
frontend/tmp/

# Vercel local project metadata
.vercel/

# Accidental root lockfile (repo does not have a root package.json)
/package-lock.json
