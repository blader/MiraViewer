# Copy this file to `frontend/.env.local` (DO NOT COMMIT .env.local) and fill in values.
# Vite exposes only variables prefixed with VITE_ to the browser.

# Google Generative Language API key (will be shipped to the browser)
# Option A (recommended): Vite-prefixed variable
VITE_GOOGLE_API_KEY=
# Option B: if you already export GOOGLE_API_KEY in ~/.zshrc, MiraViewer will pick it up too.
# GOOGLE_API_KEY=

# Optional: override model names
# Examples (update as needed):
# VITE_GEMINI_ANALYSIS_MODEL=gemini-1.5-flash
# VITE_NANO_BANANA_PRO_MODEL=nano-banana-pro-preview
VITE_GEMINI_ANALYSIS_MODEL=
VITE_NANO_BANANA_PRO_MODEL=
