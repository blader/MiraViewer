# Segmentation driven by visual golden benchmarks

> Historical plan/evidence. This preserves its original task and may include unfinished proposals. Current status belongs to the [implementation ledger](../active/2026-09-02-full-codebase-audit-implementation.md).

## Latest checkpoint — exact pruning passes the real editor

The production one-thread run completed with **75 observed frame deliveries instead of222**, preserving the original221-frame source context. Both certified endpoints matched the full saved result: forward155 and reverse82. The final selected mask is byte-identical: **20,990 voxels**, SHA-256 `b6dd0e3427b21207a938bf54fffc7ecc11a9008e9bd6f55b2617f289c986d523`. All111 foreground marks remain selected, there are no background marks or nonbinary values, and the serialized mark hash is unchanged. No model threshold, source normalization or marked voxel was adjusted to obtain this result.

The change skips propagation only beyond a certified empty **editing-grid** plane, after support and hard marks, for an explicitly foreground-connected consumer and a narrow proven grid mapping. It does not treat unobserved frames as model output. Default/raw diagnostics and multi-plane corrections still traverse their full context. The parent validates observed order, its own acknowledgements and the child's endpoints; the adapter independently reconciles those endpoints with its empty-plane certificates. Barrier memory encoding and cleanup still complete, and each direction has fresh history. Normal execution remains explicit WASM1; CPU4 remains diagnostic-only.

### Real workflow evidence

- **Completion and work:**75/222 deliveries is147 fewer, or66.2% fewer. The elapsed Auto-fill UI interval was approximately505.34seconds (8min25sec), including preparation and publication. This is one actual run on the shared machine, not a controlled speedup claim. Interactive latency remains a limitation even though the eliminated work is exact.
- **Browse during prediction:**AX97 → AX98 changed actual canvas pixels; returning toAX97 restored its exact hash. The same worker/job continued throughout. This proves functional browsing, not frame pacing.
- **Metadata:**a successful scoped result saves `contextLimited:true`; unknown raw boundary/clipping counts are omitted. Turning Auto-fill off preserves this notice. Undo restores the preceding unknown legacy metadata; Redo restores true, with identical mask and mark bytes in every state.
- **Cancellation:**a new optimized job was stopped after three real frame deliveries. Its owner terminated the worker, and the completed selection, all marks and context metadata remained unchanged. This is parent termination evidence, not an OS child-liveness measurement.
- **Reload:**a full navigation, reconstruction and editor reopen restored the identical mask, marks and context notice. Hydration did not start inference. An attempted Edit click before reconstruction finished found no control; after observing the ready state, the normal click succeeded.
- **Visuals:**`e2-prune-baseline.jpg`, `e2-prune-running.jpg`, `e2-prune-completed.jpg` and `e2-prune-comparison.png` were each inspected individually at original detail and immediately reported. The normalized actual-app comparison uses unscaled crops with a12-pixel center translation for the new24-pixel notice row. The three-plane contours and interior MRI texture are unchanged. These are bounded regression/UX passes, not independent whole-volume anatomical truth. Coarse contour steps and a small 3D preview remain visible limitations.
- **Browser provenance:**headed Chrome151, `ANGLE Metal Renderer: Apple M4 Max`; cross-origin isolated with SharedArrayBuffer available. No software-renderer or smoothness claim.

Private evidence remains under `frontend/tmp/segmentation-golden/browser-integration-20260829/`: `e2-prune-baseline.json`, `e2-prune-result.json`, `e2-prune-history.json`, `e2-prune-cancel.json`, `e2-prune-reopened.json`, `e2-prune-cleanup.json`, the four image sources above, and `compare-pruned.py`. Source hashes were frozen in the baseline/result receipts. No MRI data or image evidence was staged or published.

Cleanup **CLOSED**: owned tab1997584159 is closed, the owned-tab list is empty, all11 unrelated tabs present before closeout remain, Chrome PID62569/start time are unchanged, and the development server still responds on43124. Temporary observation wrappers were removed before reload. No primary browser restart occurred.

### Independent regression coverage

- Focused controller/runtime/transport suites:148 tests passed; adapter/geometry:50 passed; editor integration:50 passed; selection hook:75 passed. Final `npm run check` passed:2,828 tests passed,54 gated tests skipped,170 test files passed and6 skipped. TypeScript, lint, production build and the model-asset verifiers passed. Heavy validation ran separately from inference and packaging.
- A private saved-output replay exercised the actual adapter, geometry proof, categorical transfer, support/hard-mark enforcement and connectivity helper with explicitly simulated transport of pinned existing logits. E3 uses40/64 deliveries and retains38,490 voxels, all103 marks and the endorsed1,520-pixel AX96 section exactly. E4 remains66/66 and retains50,885 voxels and all106 marks exactly, with unchanged full metadata. The replay does not claim fresh model, worker, browser, anatomy or latency evidence.
- Replay receipt: `empty-plane-replay-20260830/receipt.json`, SHA-256 `0f6e8b25974cf4dda534243b8fdb82c86d966ec89cb2925c75a9f32b63f1a9fe`. Its source/input hashes and full-vs-pruned masks remain private and independently reproducible.

Independent readback reconstructed every saved-state mask from its actual selected indices and verified all nine frozen source hashes. Runtime events contain75 encoder,75 decoder,73 attention and75 memory-encoder calls:298 rather than886, eliminating588 calls (66.37%). Both barrier encoders finish before their direction-release events; successful done follows both releases. Worker start → done is486.300seconds; first Auto-fill status → published UI completion is505.339seconds. Result receipt SHA-256: `4f5b5498bf7d047727675ee876da516635816f6de2cf6515cfd8b2c7413725fd`. This batch's undo/redo changes metadata, not geometry; the previous connected-selection batch separately proved geometric undo/redo.

### Final build and offline gate

`npm run package:zip` passed after privately preserving the prior generated ZIP. The new archive is84,991,838bytes, SHA-256 `86bdb39c2a0f29a4a80d7131b306267a91f2142838987ad56603aee7b597c63e`. Independent inspection verified61/61 files against fresh dist plus five launcher templates, matching byte counts, SHA hashes and executable modes; ZIP CRC passed. All38 copied assets match their sources: nine public files,26 ITK files and three ORT files. Model pins pass in public, dist and staging; all20 browser imports and17 hashed references resolve. No MRI, masks, screenshots, private source paths or unapproved files are in the archive. The static audit observed a Python bytecode cache added to staging by the subsequent smoke-server import; root removed that exact generated cache afterward. It was never in the archive. Private receipt: `offline-package-audit.json`.

The actual packaged Python handler boots at `http://127.0.0.1:43125` with correct COOP/COEP and WASM/module MIME types. The initial browser database was empty and the model cache absent; neither was deleted or reset. External page requests were blocked while allowing this loopback origin. A harmless external probe produced a captured `Network.loadingFailed` event with `blockedReason:inspector`; local app assets loaded. The individually inspected `offline-empty.jpg` passes the bounded startup/visual check.

**Cold offline import and model execution remain BLOCKED, not passed.** The supported browser file chooser first rejected an incorrectly shaped argument; subsequent correctly typed absolute-path attempts, including a fresh chooser, both returned CDP `Not allowed`. No alternative data injection or access-control workaround was used. The workspace remains empty and the model cache absent. The user was asked to manually import the five unmodified private DICOMs so this last gate can continue. These five files are an import/runtime smoke, not a new whole-volume accuracy benchmark.

Offline browser cleanup **CLOSED**: owned tab1997584163 is closed, no owned tabs remain, all11 unrelated tabs are preserved, and primary Chrome is unchanged. The explicitly identified offline HTTP server PID77460 remains available on43125 for the requested manual import; the original dev server remains on43124. Evidence: `offline-runtime-blocked.json`, `offline-network-events.json`, `offline-cleanup.json`, `check-after-empty-plane-pruning.log`, and `package-after-empty-plane-pruning.log`.

`npm run doctor` is **not green**: nine `async-await-in-loop` warnings remain. All nine are explicit cooperative `yieldToMain()` scheduling points in validation, connected traversal/normalization or source copying, not independent serial network requests. Removing or parallelizing them would weaken responsiveness, cancellation checks or shared traversal state. No suppression or score claim was added. The existing large main bundle (2,736.95kB before gzip) remains a separate performance warning. Existing anatomical limitations and the user-disputed yellow labels are not reclassified by these checks.

## Prior checkpoint — full CPU4 run times out; reduce provably irrelevant work

The connected-selection and default-window fixes below remain validated. A normal-editor CPU4 candidate was then exercised using the exact saved E2 source and marks. It passed cancellation during decoder initialization, cancellation after one real returned frame, immediate retry, and slice browsing without restarting inference. AX97 → AX98 changed actual canvas pixels; returning to AX97 restored its exact canvas hash. All cancelled attempts retained the exact 20,990-voxel mask and all 111 foreground marks.

The uninterrupted candidate **did not complete**: 181 of 222 expected frame deliveries arrived, followed by more than 120 seconds without progress before encoder execution at source index40. The existing inactivity guard terminated its worker. Its last event was at page time1,157,387 ms; termination at1,289,524 ms, approximately132 seconds later and1,055 seconds after worker start. Browser diagnostics also timed out during this interval and recovered without a browser restart or new window. An offered blank recovery window was not opened, and the permission request was withdrawn when the existing connection recovered. The saved mask and seeds are unchanged; no incomplete prediction or new coverage metadata was published. This is a **failed full-run qualification**, not a performance or fidelity pass. Factory SHA `f45f94f83c07bf08a5f76ce4d3d2632269c7c59f785dc76044ce86ce0def529d`; worker SHA `c3e4db3b09f6d7926c5ec513ef2c8438ecdfd49cee333586cadda4c05e367d85` stayed fixed during the batch.

Private evidence: `browser-integration-20260829/e2-auto4-cancel.json`, `e2-auto4-running-receipt.json`, `e2-auto4-incomplete.json`, and `e2-auto4-cleanup.json`. Individually inspected sources: `e2-auto4-baseline.jpg` PASS saved-state baseline; `e2-auto4-running.jpg` PASS bounded running-state composition; `e2-auto4-timeout.jpg` FAIL completion/responsiveness, with intact anatomy, marks, a visible error and Retry control. No temporal smoothness claim is made.

Cleanup CLOSED: owned tab1997584124 was closed, diagnostic constructor/observer changes were restored, and no owned tabs remain. All 11 unrelated tabs present immediately before closeout remained afterward. One ID from the initial snapshot was absent before closeout; no operation from this validation targeted it. Primary Chrome PID62569 and its original start time remain unchanged.

### Reassessment and next production-shaped proof

- **Failed outcome:** full-acquisition processing is still too slow and exposed a genuine inactivity timeout. Short prefixes are not representative of mature tracking: attention grows from1,028 to7,196 tokens over the first seven calls, then caps at7,232 rather than growing indefinitely with acquisition length.
- **Environment evidence, not attribution:** a post-event snapshot showed128 GiB RAM, approximately126 GiB used including54 GiB compressor,28.84 GiB swap used,99.3% CPU busy and load averages around53/50/37 on16 cores. Approximately45 seconds showed2.61 GiB decompressed and1.10 GiB compressed, but no new swap-outs. These overlapping figures must not be added. They demonstrate heavy competition/reclamation, not the cause of the preceding stall. No unrelated workloads were stopped or modified.
- **Retained authority:** the hook owns hard marks, support, connected-component filtering, annotation/history and publication. The model still proposes tissue. Exact raw-model regression remains full by default. The saved successful WASM1 result is not invalidated by the failed CPU4 candidate.
- **Decision:** hold automatic CPU4 adoption and restore explicit one-thread production execution while keeping its explicit diagnostic capability. Do not extend the inactivity timeout, retry models automatically, change source normalization, or accept incomplete raw coverage as complete.
- **Structural alternative:** stop causal propagation beyond a certified, completely empty **editing-grid** plane when the final foreground-connected selection cannot reach the far side. This removes irrelevant model work instead of changing the model, cropping away unknown tissue, or weakening fidelity checks. A simple fixed ROI, empty native slice or connectivity-filtered incomplete prefix is not an acceptable substitute.
- **Measured opportunity:** the full saved E2 raw mask has its first empty editing planes atz82/z155 around all111 foreground marks atz96. Including both barriers requires74 unique planes /75 deliveries rather than222, saving147 deliveries and588 of886 graph calls retrospectively. Independent full-mask traversal still gives the exact20,990-voxel hash. E3's cropped context permits40/64 deliveries; E4 has no barriers and remains66/66. These are work counts from observed masks, not latency measurements or independent anatomical truth.
- **Restricted implementation contract:** explicit foreground-connected consumer opt-in; one actual marked native plane, including all background corrections; certified integer-phase axis mapping and unit tracking-axis step; full finite/support source validation before inference; complete inverse-projected editing plane after support and literal marks but before connectivity filtering; no foreground marks beyond a stop. Coarse tracking strides and noneligible mappings retain full processing. The barrier frame finishes its normal memory encoding/release, then the opposite direction starts with fresh history. Preserve original `frameCount` and all source values.
- **Coverage and metadata:** carry a named stop decision through the existing consumed acknowledgement, validate contiguous observed intervals and acknowledged endpoints on both sides of transport, and leave uncomputed `seen` entries zero. The adapter must reconcile worker coverage with its own certificates before returning anything. Omit unknown raw boundary/clipping counts when tails are actually skipped; do not label partial counts complete. Persist `contextLimited: true` for that scoped prediction. Full traversal keeps existing metadata semantics.
- **Forecast/falsifier:** E2 should reproduce the exact saved final mask while executing75 deliveries; E4 must still execute its entire domain. Any changed foreground-connected voxel, lost mark, hidden invalid future source sample, spoofed coverage acceptance or approximate boundary test rejects the optimization. Revert to full traversal for an ineligible geometry rather than loosening the proof.
- **Next evidence:** focused controller/transport/geometry/hard-mark regressions, independent proof review, frozen full-by-default raw checks, then one real editor prediction with unchanged saved marks and exact output/hash/history/reopen checks. Rebuild the stale downloadable ZIP only after code stabilizes; its old Aug24 archive lacks the new model/runtime. Five unmodified private DICOM files were copied and hash-checked for a later cold offline smoke, not a whole-volume anatomical benchmark.

## Prior checkpoint — connected selection passes the real E2 editor workflow

The 26-neighbor postcondition is implemented in `seedConnectedSelection.ts` and applied by the existing selection hook **after** support and hard Add/Remove enforcement, on its private unpublished buffer. It retains every component containing a foreground mark, not merely the largest component. It does not smooth, fill holes, dilate, alter source MRI, or migrate existing masks. Cancellation and stale ownership cannot publish its temporary visitation markers. The primitive's 69 focused tests and the hook's 73 focused tests passed, as did the focused lint checks. Source helper SHA-256: `ac4c0fbc272e63ce5b21606f269220586af26765ef007c2ce7086ce0ba18adb1`.

Independent geometry and the actual production helper agree byte-for-byte on four saved examples: E3 retains 38,490 / removes 2,115; E4 retains 50,885 / removes 84; the endorsed E1 stored axial example retains all 63,384; E2 retains 20,990 / removes 7,882. Every foreground mark survives. Endorsed E3 AX96 and E1 AX102 are byte-identical. These are regression/geometry checks, not new anatomical ground truth. Private receipt: `postcondition-geometry-20260829/implementation26-receipt.json`, SHA-256 `9431a99063632b93f7bdce7c0183c40410e6e2276b11b7d259aea5670305f037`.

### Actual application validation

The same E2 examination and 111 foreground / zero background marks completed a fresh full-source Auto-fill through the real editor in the user's headed Chrome. The new helper was confirmed loaded in the app; no source changes occurred during the run. The saved result is **20,990 selected voxels**, zero nonbinary values, zero missing foreground marks, and mask SHA-256 **`b6dd0e3427b21207a938bf54fffc7ecc11a9008e9bd6f55b2617f289c986d523`**, exactly matching the independent expected result. The repeat's serialized seeds hash stayed unchanged; its serialization differs from the earlier canonical-array seed receipt, so the different serialization hashes are not a mark-change claim.

- Undo restored the exact 28,872-voxel pre-change mask and its seeds; Redo restored the exact corrected mask and seeds.
- A full page navigation, reconstruction and editor reopen restored the corrected mask and marks exactly. Hydration did not automatically run inference.
- The application was rendered by headed Chrome 151 on `ANGLE Metal Renderer: Apple M4 Max`, not software rendering. This is not a motion/frame-budget claim.
- Completion was observed by **1,884.1 seconds (31.4 minutes)**. Forty-five-second observation intervals do not establish precise inference duration. The machine had substantial concurrent workloads, so this is not a controlled before/after performance comparison. It nevertheless **fails interactive-latency expectations**; no performance improvement is claimed for this run.
- One later browser-command timeout recovered with a fresh read of the same owned tab; Chrome was not restarted, and no new model run was started during recovery.

Private artifacts under `browser-integration-20260829/`: `e2-connected-ui-result.json` (SHA-256 `d93395d51635ad9cdd2536dffc52efe9ac3c50b5e2e8754ddc327957094e9b58`), `e2-connected-history.json`, `e2-connected-reopened.json`, and `e2-connected-cleanup.json`. The completion receipt deliberately excludes unrelated browser URLs. No MRI evidence was staged or published.

### Current visual verdicts

Each source below was individually inspected at original detail and immediately reported:

- `e2-connected-baseline.jpg` and `e2-connected-baseline-3d.jpg`: FAIL whole-selection coherence; saved unmarked islands remained visible before rerunning, proving there was no silent stored-mask rewrite.
- `e2-connected-completed-3d.jpg`: PASS the bounded disconnected-island correction; central selection and interior texture remain, detached patches disappear. Overview contour stair-stepping and a soft volume surface remain limitations.
- `e2-connected-comparison.png`: PASS the normalized actual-app before/after comparison. Crops are unscaled; a 12-pixel canvas-center translation compensates for the new 24-pixel context-warning row. The central boundary/interior are preserved; the distant patches disappear. This does not establish complete clinical anatomy.
- `e2-connected-ax138.jpg`: PASS the specific previously erroneous distant axial selection check; its ventricular-adjacent outline is gone while the central selection remains in the other views.
- `e2-connected-reopened-default-window.jpg`: FAIL default display fidelity, despite exact persistence. Bright tissue is washed out and the 3D anatomy is excessively opaque.

Cleanup: **CLOSED**. Owned tab `1997584014` was closed, the stale-owned tab list was empty, all 15 pre-existing unrelated tabs remained (16 unrelated tabs at closeout), and the primary Chrome process retained PID 62569 and its original start time. The local server and saved MRI data remain intact.

### Next bounded corrections and experiments

1. **Display window authority — implemented and visually verified:** header-only evidence explains the washout. E2's middle acquired frame uses center/width 656.5/1313 (`[0,1312]`), whereas its marked frame uses 1871/3742 (`[0,3741]`); 182 distinct source window pairs exist. The editor correctly uses `volume.displayWindow`, but that volume previously chose one middle informative frame. It now encloses valid linear-VOI ranges across informative *loaded* frames, preserving fallback, width-one, manual-override, raw-plane and source-pixel semantics. No percentile/anatomy tuning or additional decode is needed. Four focused regressions failed before the change; all 29 native-volume tests and focused lint passed afterward. In the actual reopened E2 app, `e2-source-window-default.jpg` and normalized `e2-source-window-comparison.png` PASS the bounded saturation/detail correction. The new viewport was 56 pixels shorter, so unscaled crops use a 28-pixel center translation; MRI pixels were not stretched. Manual width survives browsing and Auto-fill off; Reset restores the new default. Stored mask and seeds remain exact. Receipt: `e2-source-window-receipt.json`. DOM slider values are quantized and are not exact source-window readbacks. Cleanup CLOSED for tab `1997584038`; all prior unrelated tabs preserved.
2. **Warning ownership — implemented; real new-prediction browser proof pending:** disabling Auto-fill previously cleared transient `contextLimited` status while leaving labels unchanged. Optional mask-owned boolean evidence now persists through history, cancellation, edits, save/reopen, backup and transfer, following the existing clipping-metadata path. Successful suggestions establish true or false; absent legacy evidence remains unknown. No proposal-cache authority, automatic backfill, provenance fields or hydration inference were added. The initial-prediction boundary notice no longer claims the filtered mask itself touches an edge; its raw count remains transient. Three hundred focused tests passed across hook/editor, IndexedDB API, backup, transfer, migration and viewer save-confirm-reopen cases, plus focused 17-file lint. The actual saved E2 row predates this field and was deliberately not rewritten to manufacture a browser pass.
3. **CPU parallelism experiment — frozen regression and cancellation pass; production adoption pending:** the actual Chrome WASM4 arm preserves every raw binary decision on the eight-frame prefix and the complete 63-source-frame regression (64 returned frames), using the unchanged numerical tolerance and no hard-mark or connectivity modification. Full projected raw-mask SHA-256 remains `ad3d61b9b0f5ef87d6c5b6f640aa55a71475948ffb8adde85b8dba468f8acffb`; all 103 foreground marks are present. Full tracking took 256.781 seconds after the prefix warm-up. Short tracking observations were WASM1 46.731/36.634 seconds and WASM4 37.153/20.104/28.443 seconds. These small interleaved samples on a shared machine are diagnostic, not a portable speedup estimate. Session execution dominates the first single-thread run (46.145 of 46.731 seconds); before-graph scheduling accounted for only 0.146 seconds. Four threads created exactly three observed pthread workers and used 260,177,920 bytes of observed WASM linear-memory capacity versus 256,180,224 for one thread; this is neither total browser memory nor an admission guarantee. All four sessions released successfully. An explicit cancel immediately after the first returned frame also released all four sessions and requested termination of all three pthread workers before the diagnostic owner terminated. An earlier late cancel arrived after completion and is correctly archived as a completed repeat, not cancellation evidence. Private receipts/artifacts: `wasm-thread-probe-20260829/wasm{1,4}-*-receipt.json`. Raw `Target.setAutoAttach` is unsupported by the connected browser tool; no alternate connection was used, and OS-level child-thread liveness is not asserted. Production is still WASM1 at this checkpoint.
4. **Possible later pruning, not implemented:** a finalized empty *editing-grid* plane with no foreground marks beyond it is a valid 26-neighbor separator, if future frames cannot alter that plane or the retained side. An empty native frame alone is unsafe with coarse sampling (`[1,0,1]` can become adjacent `[1,1]`). Do not prune correction preparation: even future background-only conditioning can affect retained-side predictions. Do not use a connectivity-filtered incomplete prefix as the barrier, hide uncomputed frames as observed empty outputs, or imply raw boundary/clipping metadata remains equivalent. Full raw-model regressions stay unchanged.

Latest full validation: lint and TypeScript pass; production build and both public-model asset verifiers pass; 2,731 tests pass, 54 skip (170 passing files, six skipped). Two test-only waits were corrected to observe completed publication rather than merely the start of an asynchronous operation. Existing React `act` warnings and the large main bundle remain. Log: `browser-integration-20260829/check-after-connected-fixes.log`.

Thread-probe cleanup: the sole owned tab `1997584094` is closed and the owned-tab inventory is empty. Primary Chrome PID 62569 and its start time are unchanged, and the local server still returns HTTP 200. Eight IDs from the initial unrelated-tab snapshot were absent at closeout amid shared-session changes; this validation issued no close/navigate operations against those IDs. Do not claim the entire unrelated-tab inventory remained identical. The cleanup receipt records the mismatch rather than reopening or modifying unrelated tabs.

The broader work remains incomplete: normal-editor CPU acceleration, durable context-warning browser proof, offline operation, measured resource admission and wider anatomical review still require evidence. The changed default-window visual proof is complete. No commit, push or merge was performed at this checkpoint.

## Earlier checkpoint — real E2 prediction completed; disconnected selections exposed

Direct desktop control was explicitly approved and used without restarting the user's browser. The completed GPU-memory experiment was recovered from the existing page rather than rerun: all 64 exchanges covering 63 source sections completed in 216.157 seconds. It selected 40,604 voxels versus the frozen CPU reference's 40,605. The single omission is full-source XYZ `[249,247,114]`: CPU score `+0.00204606`, GPU-memory score `−0.000540622`. All 103 foreground marks survive, and floating outputs meet the previously declared tolerance, but **the frozen zero-mask-difference regression fails**. All four sessions and both owned workers were released. The original export, decoded artifacts, independent comparison, hashes and timings are saved privately under `browser-integration-20260829/e3-gpu-memory-*`. Production remains WASM-only; these uncontrolled instrumented timings do not establish ordinary-user speedup.

### Important fixture-identity correction

The actual application was displaying **E2**, not frozen E3. The resident DICOM headers identify the visible examination as E2, and an independent read of the saved selection's series identity confirms SHA-256 `51687822d5e4fdeb2ab36439f12b0b0a01ca571f2f0228a4b31f70bc5091be5e`. Frozen E3 instead has series SHA-256 `3e5e1e5e1c6707ef377f34ad055933a7ca3d98f4ab258264a02f2a21717fc7c3`. Both have 512×512×221 native dimensions, so dimensions alone were insufficient provenance. Earlier UI captures showing this examination—including filenames containing `e3`—must be interpreted as E2; filenames and bytes are preserved. The separate frozen-input E3 diagnostic receipts remain E3 evidence. The actual overview's intensity span is 3,737, not the frozen E3 source range; it is not a new full-source pixel-range measurement.

### Completed actual-editor proof

The imported E2 overview is 256×512×221 at 0.859375×0.4296875×1 mm. Restored interior marks plus one additional interior Add stroke were processed through the real Auto-fill control. The configured WASM1 path completed; completion was observed by **807.6 seconds** after the initiating toggle. This is an observation bound including control/scheduling overhead, not a controlled inference benchmark. Native slider navigation moved the axial image, number and slider together from 97 to 98 and back without restarting the prediction.

The saved mask contains **28,872 selected voxels**, including all **111 foreground marks**, with no nonbinary values. There are no background marks in this baseline. An exported sparse representation independently reconstructs the exact 28,966,912-byte mask, SHA-256 `1737b11451b461f9c22f27cf024e7b2a6e3fc6b5c70c8dd8a84c1cffeac1aa57`. The private export is `e2-ui-completed-export.json`, SHA-256 `ae519c376160af312b1c3075af34ef50b466eb7e11c05ae363582f8d8a2dcd15`. This is real integrated output, not independent anatomical ground truth. Saved `modelKey: manual-seeded-v1` does not independently identify learned completion; the causal evidence is the observed workflow plus stored mask expansion.

- Undo removed the proposal while retaining all 111 marks; Redo restored both mask and seeds exactly, verified from IndexedDB hashes.
- A brush-only Remove stroke on a detached lower island excluded 29 voxels, saved 29 background marks, preserved all foreground marks, and left zero background violations. Undo restored the original mask and seeds exactly. The temporary Remove mark was **not** used for a new model run and is not left in the saved baseline.
- Private receipts: `e2-ui-full-run-observations.json`, `e2-ui-history.json`, `e2-ui-correction.json`. The original downloaded files remain intact.
- Full remount/save-reopen, offline operation, repeated learned inference and measured resource bounds remain unfinished. A completed prediction is not completion of the broader task.

### Visual finding and bounded next change

The individually inspected actual-app sources `ui-desktop-fill-complete-ax97.jpg` and `ui-desktop-fill-ax97-source-window.jpg` show a coherent central axial selection retaining bright and dark interior tissue. The default display window washes out interior texture; a manually widened window and 2× zoom make review possible without changing source values or labels. The overview contour remains visibly stepped. This is not a normalized fidelity comparison with the endorsed E3 example.

The individually inspected `e2-ui-ax138-extent.jpg`, `e2-ui-cor236-extent.jpg`, and `e2-ui-sag129-extent.jpg` **fail whole-target coherence**: distant ventricular-adjacent strips and detached patches accompany the central body. These findings do not depend on the disputed yellow outlines. A geometry-only audit independently finds 13 components under six-neighbor connectivity and nine under 26-neighbor connectivity. Both definitions give the identical sole foreground-connected component: **20,990 voxels, all 111 marks**, bounds `[111,204,83]`–`[146,266,114]`. All **7,882 other selected voxels** are unanchored. The lower z74–79 island contains 2,518 unanchored voxels; every one of the 405 selected voxels on z137 is in another unanchored component. Receipt: `e2-ui-connectivity.json`, SHA-256 `a93139560a319948019b037dec36e622bbad21e2cd1b421fb709f67fb2a8af37`. Geometry is not an anatomical adjudication, but it supplies an independently testable relationship to the user's marks.

Architecture decision: **keep the learned proposal, constrain application selection by actual 3D connectivity to Add marks**. The structural mismatch is treating every temporal tracker reappearance as belonging to the requested tissue. A replacement 3D model would invalidate the strong central example and introduce a new unproven footprint; threshold changes could alter the central boundary and interior. The bounded alternative is the union of 26-neighbor components containing explicit foreground marks, applied by the existing selection/publication owner after acquired-support and hard Add/Remove enforcement. Keep multiple separately marked components; do not smooth, erode, dilate, fill holes, or truncate by a hand-chosen slice range. Existing saved selections and original raw-model regression artifacts must not be silently rewritten.

Production-shaped proof required before accepting this change:

1. Synthetic primitive and hook tests for separated marked regions, diagonal connectivity, row/plane boundaries, holes, authoritative background cuts, cancellation, stale ownership, and exact undo/redo.
2. Apply the same rule to saved E2, E3 and E4 outputs without rerunning or altering the source model. The endorsed E3 axial example must remain unchanged; any alteration reopens the design rather than retuning the test.
3. Complete another actual-editor run and inspect matching axial/coronal/sagittal states plus a normalized before/after comparison. Confirm that the central body is unchanged and the observed distant islands disappear. Preserve the previous result as the baseline.
4. Finish remount/persistence and correction checks. Performance remains unacceptable at the observed full-run latency. A separately controlled browser WASM4 diagnostic is feasible to investigate, but is not yet an implementation, proof, or production policy change; the previous Node file-URL bootstrap failure occurred before any model graph and does not answer browser support.

Abandon or revise this rule if it removes the endorsed body, loses explicit marks, breaks legitimate thin connections after overview projection, or still leaves the visually identified unmarked islands. No anatomical acceptance or GPU-admission gate is relaxed by this experiment.

Additional observed UX issue: the limited-analysis-region notice disappeared when Auto-fill was turned off, although the resulting mask remained. Its persistence semantics need separate review; the existing persisted clipped-native-voxel metadata describes a different fact.

### Browser ownership and evidence closeout

Concurrent Chrome activity changed the active tab/window during native capture. One wrong-surface capture and one clipped capture were rejected; neither supports a product verdict. Expanding the owned window restored a complete, separately inspected capture. Later native capture returned `cgWindowNotFound`; the primary Chrome process remained running. The supported Chrome connection recovered the exact owned tab `1997583979` and closed it and its docked tools. A fresh scoped audit found no localhost MiraViewer tabs, confirmed the old benchmark tab `1997583819` was gone, and retained **all 14 unrelated tabs**. The window had become shared with another workflow, so its unrelated content was preserved. **Cleanup: CLOSED.** The server and imported MRI remain available. No primary browser restart, commit, push or merge occurred.

## Earlier recovery checkpoint — partial GPU result, control lost again (historical)

After the user reinstalled the Browser plugin, actual DOM inspection succeeded for both the blank recovery tab and MiraViewer. Both old owned tabs (`1997583683`, `1997583727`) were closed through the supported browser API; a fresh audit found no remaining owned tabs and confirmed every other tab was preserved. This supersedes the earlier cleanup blocker for those two resources.

The frozen GPU-memory-only experiment then started in new owned tab `1997583819`. The exact factory, controller and diagnostic hashes still matched the prepared experiment. The page was visible, cross-origin isolated, and used an Apple Metal 3 nonfallback adapter. At the last successful read, 48 of 64 required frame exchanges had returned at 157.189 seconds, with **one cumulative output-domain binary difference**. The zero-difference regression requirement is therefore already falsified; neither a full result nor the location/direction of that changed voxel has been recovered. The 31-frame timing observation puts image encoding at roughly 2.56 seconds mean per graph, versus 74 ms for memory attention, including scheduling overhead. This is diagnostic timing, not a controlled normal-use performance claim. [V8 documents debugger-induced WASM tier-down](https://v8.dev/docs/wasm-compilation-pipeline); whether this affected the installed browser's tracking worker remains unmeasured.

Browser control subsequently disconnected. Fresh supported discovery still found the same test tab, but reclaiming it timed out. **Do not restart this model job merely because observation failed:** its current completion/termination state and final artifacts must first be recovered or independently verified. The durable partial observation is `frontend/tmp/segmentation-golden/browser-integration-20260829/e3-gpu-memory-control-loss-observation.json`; it deliberately is not a completion receipt or a mask artifact. No prediction was published to the application, no provider was activated, and no source/controller/threshold changed. Cleanup is currently **BLOCKED** for tab `1997583819`. The user has been asked whether direct desktop control of Chrome may replace the repeatedly failing extension connection; no such alternate control has yet been used.

A read-only broader-corpus inventory also established that E4 has a pinned native 65-frame EfficientTAM input and source-only green-mask readback, but is not untouched holdout data and has no full-depth/browser proof. E1 original-acquisition and E2 need complete native input/range preparation. Their existing engineering Add marks are not user-drawn clinical annotations. Public corpus entrypoints reject disputed/unclassified yellow outlines; ignored legacy scoring functions still accept historical frozen metadata when invoked directly and must not be reused for new accuracy claims. The old yellow set remains USER-DISPUTED.

## Earlier 29 August live-browser checkpoint — historical, superseded above

Browser access was restored for the completed checks below. The real local application at `http://localhost:43124/` imported all **1,405 protected sample DICOM files** through its upload UI. No browser setting change was required. MRI, annotations, predictions and captures remain under ignored private paths; the public model directory contains only the allowlisted Apache-2.0 model assets. The user's primary browser was not closed or restarted. At the latest checkpoint, a new browser connection is detected but tab control times out. The prepared GPU-memory experiment has **not run**; reconnecting does not invalidate the completed, saved evidence.

The native-context proposer is now connected to the existing selection hook, which remains the single publication, hard-mark and undo authority. Source preparation, model execution and categorical projection cannot replace the accepted MRI volume. Failure or cancellation does not invoke the old classifier. This wiring is **not yet a completed real-editor acceptance test**.

Actual Chrome 151 on the Apple M4 Max exposed a provider-specific correctness defect: with identical source pixels, prompts and saved encoder features, WebGPU decoding added 22 pixels to the synthetic anchor. Browser WASM reproduced the saved reference exactly. Cross-feeding GPU encoder features into WASM decoding also preserved that anchor, but a subsequent actual E3 hybrid trial changed five output decisions on later sections. The latter was stopped after 27 returned frames once its zero-difference regression gate was falsified. Its partial, canceled receipt is `browser-integration-20260829/e3-hybrid-partial-receipt.json`, SHA-256 `efafddcb0129a4bec51b9d5d4b6b94b9a9db979e29190c669dfffc061c6a250d`. It is not a passing full-stack result. No model threshold, source preprocessing, prompt or golden mask was adjusted to hide this difference.

Production admission therefore selects **WASM only**. The disposable tracking worker uses one WASM thread, matching the previously proven portable configuration. GPU and hybrid placement remain explicit diagnostic options, not automatic fallbacks. The actual-browser E3 regression now passes all 63 frozen source sections: 40,605 selected voxels, zero mask differences, all 103 foreground marks selected naturally, no unsupported selections, and all raw scores within the unchanged numerical tolerance. Its 64 frame exchanges include the duplicate anchor. Receipt: `browser-integration-20260829/e3-wasm1-receipt.json`, SHA-256 `b6590e983d91ec7e5e874168de295c8b212584b7527b9f3acde31cae05a33533`; output mask SHA-256 `ad3d61b9b0f5ef87d6c5b6f640aa55a71475948ffb8adde85b8dba468f8acffb`. Completion followed disposal of all four sessions and termination of the owned workers. This is output-regression fidelity, not independent anatomical truth, a complete 221-frame editor prediction, or a memory-admission measurement. The diagnostic took 432.5 seconds including comparisons; instrumentation and concurrent load prevent a controlled performance claim.

Learned selection now has an explicit conservative *estimated* operation envelope: at most one quarter of reported device RAM, capped at 3 GiB; 1.5 GiB when that information is unavailable. This is not measured free memory or a safety guarantee. Its 1 GiB fixed runtime/compilation allowance is separate from dynamic temporal/prompt attention, native input, accepted display/history, cache growth, transport, and pre-trim publication buffers. The ordinary reconstruction limit remains 512 MiB. The initial 2 GiB draft did not accommodate the real 256×512×221 retained overview; the high-RAM cap was revised explicitly, rather than changing the source resolution or dropping marks. The source-loading mismatch is fixed: the admitted learned operation passes its budget to the existing native loader, retaining the same source geometry, identity, cancellation and ownership checks. A real-loader synthetic regression using the corpus dimensions proves the default 512 MiB gate rejects before decoding, while the exact 637,258,560-byte native-load estimate permits all 221 native frames; patient-identity mismatches still reject. This does not change ordinary reconstruction or independently acquired 2D-series admission. The related 160 focused tests passed.

Real UI inspection also found and verified a rendering fix: a blank first source frame carried center 0 / width 1, making the whole 3D background opaque. Native assembly now chooses a valid informative contributing frame near the region midpoint for the volume window. Original MRI values and each original slice's own window remain unchanged. Individually inspected private captures and the normalized before/after comparison are under `browser-integration-20260829/`; `volume-window-comparison.png` passes the bounded window/background correction, not tumor anatomy. The initial `editor-native-ready.png` failed wide-screen ergonomics. The revised ultra-wide editor gives its four views equal-width columns, keeps Auto-fill beside the editing controls, and reserves the native-detail row to prevent the first mark shifting the MRI canvas. `editor-layout-comparison.png` verifies the bounded composition change; Clear and Undo preserve identical grid geometry. Separate 1920×1080 and 430×932 captures show no horizontal overflow. The mobile capture proves only the first viewport, not every lower panel, and the changed 3D selection states are not a rendering-parity comparison.

The actual editor now accepts Add marks, prepares its native source, starts learned inference, and remains browsable while inference runs. Stop restores brush-only editing without publishing an incomplete prediction; the two test marks survive reopening. However, the 221-frame CPU attempt reached only 47% after 438.8 seconds and was canceled. Receipt: `browser-integration-20260829/ui-cpu-partial-receipt.json`. **No completed full-volume prediction or successful filled-mask correction workflow has been demonstrated in the real editor.** This latency is not acceptable as a finished interactive feature.

A local development confounder is repaired separately: Vite polling was watching 35,094 private research files, including a Python environment, while the idle server consumed roughly 250–350% CPU. Its watcher now ignores only the exact `frontend/tmp/` tree and the lint checker watches `src`; the existing server stayed running. Subsequent idle snapshots were roughly 4–5% CPU. These observations establish a watcher repair, not a measured segmentation speedup. Private diagnostics still serve over HTTP, but edits there require explicit new/frozen diagnostic URLs because HMR watching is deliberately disabled.

The final bounded provider-placement experiment is prepared: only temporal memory attention uses WebGPU, while image encoding, decoding and memory encoding remain WASM1. The unchanged frozen input, marks, score tolerance and exact final mask gate govern it. First out-of-tolerance scores must stop with explicit incomplete coverage; partial outputs cannot pass. Preparation and mock rejection tests are complete, but there is no actual model result yet. Do not enable this provider in production or keep adding placement variants if this experiment fails; reassess the interactive performance strategy using the saved fidelity and graph-timing evidence.

Projection-clipping evidence now belongs to persisted label metadata, not transient progress or proposal-reuse status. It survives failed/canceled retries, undo/redo, save/reopen, backups and grid transfers; malformed counts reject before writes. The corresponding focused suites passed 265 tests and a real IndexedDB-backed viewer save/reopen regression. Separate admission/workspace checks passed 75 tests. These overlapping focused counts are not a new full-suite total.

Next proof: complete the bounded performance decision, then finish real Add/Remove/Auto-fill with the imported MRI; inspect source-aligned boundaries and whole extent before judging accuracy. Validate cancellation, filled-mask undo, persistence, offline and repeated use. The endorsed axial example remains a regression example, disputed yellow outlines remain excluded, and unreviewed whole-volume anatomy remains unreviewed. Completed validation batches closed their owned tabs and workers. The current unstarted diagnostic tab could not be closed after browser control timed out; no diagnostic/model worker was started in it. The development server and imported data remain available.

Fresh current-worktree checks ran sequentially and passed: production build (TypeScript, Vite, and both public/distribution model-asset checks), lint, and the full Vitest suite: **2,611 tests passed, 54 skipped; 169 files passed, 6 skipped**. Remaining nonfatal warnings are the large main bundle and React `act(...)` warnings in the volume-viewer tests. All 82 tests in that file passed. These checks do not substitute for the unfinished live segmentation workflow. No commit, push or merge was performed.

**Current stopping condition: blocked, not complete.** Browser attachment failed again across three consecutive goal turns. Fresh supported discovery still lists the owned MiraViewer tab `1997583683`; both ordinary attachment and the documented reclaim path time out before returning a controllable handle. The app still responds HTTP 200. No GPU-memory diagnostic, new model job, source-image review or new visual batch started after this failure. The user subsequently approved the documented blank-window recovery. The helper opened `about:blank` in the existing Chrome profile, and fresh tab discovery identified `1997583727`; its first claim returned a handle. This did **not** establish working page inspection: the old tab still failed a 60-second attachment attempt, and a later claim-plus-DOM-snapshot call for the blank tab also timed out. The latter call does not distinguish which of its two operations stalled. The earlier statement that the new window was fully controllable was premature. No user-owned browser resource was closed or restarted.

Cleanup is **BLOCKED** for the stale MRI test tab and the approved blank recovery tab. The user has been told that closing the MRI tab is unnecessary and asked to reinstall the Browser plugin through ChatGPT's plugin UI, as the supported recovery instructions prescribe after this failure. The unresolved control failure is not evidence that MiraViewer's renderer is frozen. Resume after control is restored: close or safely recover both owned tabs, execute the frozen GPU-memory decision test, then finish the real editor and cross-plane/cross-date acceptance work above. Do not treat the saved CPU regression or green checks as completion.

Read-only review during recovery found no automatic inference/reconstruction loop on fresh mount. Restoring an accepted volume can perform finite full-volume label/support scans, but there is no runtime evidence connecting these to the attachment timeout. A separate timer audit counted 254 zero-delay graph waits for the frozen 63-frame traversal and 886 for the 221-frame traversal. Even a hypothetical 4 ms clamp on every wait would contribute only 1.016 and 3.544 seconds respectively; that arithmetic is not an upper bound on actual scheduling delay. [Chromium's inspected feature declaration](https://chromium.googlesource.com/chromium/src/+/e69d47fd0748197fc3b15550495255020a470cab/third_party/blink/renderer/platform/scheduler/common/features.h) disables dedicated-worker throttling by default, and [its March 2026 scheduler change](https://chromium.googlesource.com/chromium/src/third_party/+/4d9412b8f65a73bc2150e05e189b8a96c843d8a8) distinguishes experimental worker behavior from page throttling. Neither proves the installed browser's actual scheduling. No yield/controller change was made. If timing remains poor after the Vite watcher repair, measure same-worker timer-wait versus `session.run` durations on eight unchanged forward frames before attributing the slowdown to scheduling or adding another optimization.

## Earlier 29 August integration checkpoint — historical

The active frontend still uses the prior seeded classifier. The work below does **not** establish a deployed learned-selection feature or whole-volume anatomical acceptance.

- The exact approved model now runs through the new production TypeScript frame adapter/controller. Real `onnxruntime-web/all` single-thread WASM in Node reproduced all 63 native binary masks and both approved anchors; all saved floating outputs also match the preceding WASM proof byte-for-byte. Torch floating outputs remain numerically, not bitwise, equivalent. Production receipt: `medsam2-pilot/web-runtime-v1/production-ts-v1/run/receipt.json`, SHA `452d3da24388966ccf8aae4e6cf19f8d101a33fe4746dcd6c9c13928b3305443`; independent readback SHA `a58d70015761bb89c92fa38fbf5942df375a242cf5262748f208da349972fa40`.
- That run took 107.545 seconds. Its peak WASM heap was 256,180,224 bytes; process RSS peaked at 1,154,990,080 bytes and was 528,384,000 bytes after release. Four sessions and all nine output files closed, and controller-owned direction buffers returned to zero. These measurements **do not admit the model under the 512 MiB browser budget**. The separately pinned disabled-optimizer control is complete and not adopted: it changed two native binary decisions, took 107.101 seconds, reduced peak RSS to 860,880,896 bytes but increased post-release RSS to 787,365,888 bytes. Its WASM/controller peaks were unchanged. Comparison receipt: `medsam2-pilot/web-runtime-v1/production-ts-disabled-v1/run/comparison.json`, SHA `a9b7ab512e5b456f0e3aff2aeb76732d8658baaf55577915652a189c9d278f37`. This neither preserves the exact output regression nor establishes browser admission. No production optimizer change was made.
- An extent-only official-model trial now covers all 221 acquired E3 planes with unchanged square XY context, fixed legacy range, two positive prompts and no negative prompts. Every old 63-plane raw score and mask is byte-identical. It selects 94,249 voxels, retains all 103 foreground marks without enforcement, and selects no unsupported samples. Receipt: `medsam2-pilot/outputs/e3-full-depth-v2/receipt.json`, SHA `d6d51c7d6b82f2a6acf7820b715cea2c14c276ab888d058dd38c679f1f06fe4a`. The first wrapper failed on an anchor-file save signature before propagation; its output/failure receipt was retained, and v2 fixes only those two save calls.
- Root individually inspected the full-depth green-only COR245, SAG257 and AX96 figures at original detail. AX96 remains the endorsed compact target. The newly exposed broad continuation near source indices 120–142 and the detached upper area remain unreviewed anatomy, not automatically incorrect or accepted tissue. No clipping, threshold tuning, morphology, disputed-yellow scoring or canceled Remove mark was used to conceal them. Figures are under the receipt's `source-overlays/`; the source-only batch opened no browser resources and cleanup is closed.
- Actual brush-plane metadata now survives a stroke, auto-fill and undo/redo. Exact patient-grid plane/mark mapping and native context loading are implemented with synthetic tests. Annotation transfer omits unrepresentable plane metadata instead of rounding; migration compares valid optional metadata and preserves ineligible saved rows. A real E3 readback maps all 103 foreground marks to the approved native context with the actual patient affine, not an identity-grid substitute. The production component-medoid extractor preserves the approved two-point order exactly. These are adapter checks, not new model or browser executions.
- A separate seven-frame synthetic official-model oracle now exercises one foreground anchor, a later negative-only correction and an earlier positive correction. It preserves the anchor and all seven output sections, and changes 29 binary decisions on unmarked neighbors. **Two of the three new literal correction points remain unsatisfied in raw model output.** The real per-plane correction protocol is therefore proven to execute, not proven to produce reliable corrective quality. Explicit marks must retain authority. Receipt: `medsam2-pilot/snapshot-correction-oracle-v1/run-v2/receipt.json`, SHA `2ef779f927ed9b5f6b373c59b069b0eec81b7a413e2bfbb23e6d62e8aa9e8ff4`; independent readback SHA `ea924926c0f179006eca87f0cf4d114322b671ebc12473dcf4792e25a8420ee6`. Its failed first harness attempt remains preserved; the retry only copied aliased object-identity containers. No MRI, disputed labels, canceled marks, enforcement or tuning was involved.
- The TypeScript/worker correction implementation passed 96 focused tests and the fixed three-case WASM proof: original single-anchor traversal, no-correction snapshot, and corrected snapshot. All seven native binary masks match Torch in every case. Preparation masks are not published; conditioning and directional pointer order match; four sessions and owned tensors are released. Independent review then caught callback-triggered cancellation gaps in both snapshot and single-anchor paths. Eight additional regressions failed before the two single-anchor abort checks and passed after them. The repeated WASM proof saves all 757 graph/input/output arrays byte-identically to its preceding run. Receipt: `medsam2-pilot/snapshot-correction-oracle-v1/web-production-v2/run/receipt.json`, SHA `534dc6155d5c3d4d9fa440309b77886de8393ea43b474a687b0d2c7e9ab429da`; independent readback SHA `6eba4c866f75650f679aa33d6ffc1dd9babc9f342ccbad9011e3282152ad1947`. That run pins controller SHA `ffb1763b9d84c8b9588183d53466196f4da74d79ec053f0e10404a04f83dabf8`; the later single-pass `Map` construction removes only intermediate arrays and is covered by the final focused tests, not a new model execution. Current controller SHA is `4d5ba8d336b4e019b0cf502cdac5f6e7894a79bde8516185ea2451f55880ac15`. Raw Torch/WASM scores are not byte-identical: corrected maximum low/native error is 0.002392/0.001991, within the predeclared runtime bound; the separate strict 1e-4 diagnostic still reports differences. Runtime was 28.675 seconds for the three synthetic cases, with 1,178,042,368-byte peak Node RSS. None of these results establishes browser memory admission or improves the oracle's two unsatisfied raw corrections.
- Backup review found a real persistence bug: JSON serialization turned brush-mark typed arrays into numeric-key objects, and restore left them unusable. New v2 exports use ordered arrays; restore revives both these and old contiguous numeric-key objects into `Uint32Array`s while preserving literal order, duplicates and optional plane metadata. Invalid marks/geometry reject before the medical commit. Root independently reran all 44 backup tests plus seven model-factory tests successfully, including actual IndexedDB export/read/restore and cancellation/atomicity checks.
- The model asset loader verifies same-origin SHA/size pins and uses the existing transactional Blob store. Partial model initialization releases owned sessions. Twenty-three focused loader/factory/runtime-entry tests passed. A standalone synthetic maintainer exporter exactly reproduced all six model files and both notices; only these allowlisted public assets are now installed locally under `frontend/public/models/efficienttam-tiny512-v1/`. No MRI, mark, prediction, private receipt or patient coordinate was copied there. The final production build passed TypeScript, Vite, and both source/distribution integrity checks. The full suite passed 2,421 tests with 54 optional tests skipped; after the final cancellation/Map changes, all 196 affected controller, worker, model-factory, source-context and backup tests passed. The eight standalone Python exporter guards also passed. Existing React `act` test warnings and Vite chunk-size warnings remain. React Doctor's serial-loop and legacy-JSON-fixture false positives have narrow, documented exceptions backed by the ownership and real backup tests; no model activation or dummy import was added to satisfy a diagnostic. The preview serves the exact encoder with HTTP 200 and cross-origin isolation headers. This is asset/build proof, not an active learned editor or a browser walkthrough.
- Supported browser discovery still returned an empty list at this checkpoint. No user-owned window/profile was launched, closed or restarted. The user has been asked to reconnect browser access in Settings → Computer use. Integrated editor/GPU, cancellation, offline and memory-pressure validation remain required; saved figures and Node execution cannot substitute for them.

Next causal checkpoint: resource-admissible model execution, stable source-bound normalization and actual multi-plane correction behavior, then connect that complete path to the existing selection/history owner. Do not activate a silent fallback to the old classifier or claim helper tests close the objective.

**Scope checkpoint:** further model variants or arbitrary context/normalization tuning would not resolve the missing browser evidence or the intended full 3D boundary. Finish the current bounded port/transport checks and preserve their limitations. Do not raise the shared memory budget, adopt the disputed outlines, clip the exposed continuation, or add an unvalidated default editor path merely to report integration. The endorsed example, reliable persistence and verified portable execution remain useful, but they do not close the requested outcome.

**Local-check closure:** the bounded port/transport checks above are finished. The final changed tree passes build, lint, diff checks, all 196 affected tests, and React Doctor's local scan with zero findings. Its numeric score could not be refreshed because the score API was unreachable; do not call that a freshly verified 100. No further model processes are queued. The existing preview remains on port 43124 with its original server process, and no browser windows were opened, closed or restarted. All new integration work remains local and uncommitted; the prior TurboVac/PR-head clearance does not cover this dirty tree.

The browser-access blocker has persisted across the preceding goal turns, including the 08:44, 09:28 and 10:35 UTC completion reports, and supported discovery still returned no browser in this turn. The remaining end-to-end work now requires that external connection. Resume from the sequence below after it is restored; do not keep running unrelated model variants or silently mark the goal achieved.

### Resume at the real application boundary

1. Reconnect the supported browser containing the local application. Preserve every existing user window, profile, loaded examination and selection; do not relaunch the primary browser or use a separate automation backend to bypass the unavailable connection.
2. Verify actual model loading and execution in that browser, with the accepted volume/display and editing history still resident. Measure WebGPU or browser-threaded WASM setup, inference, repeated use and release; include source context, graph/runtime workspaces and all retained owners. The small controller-state estimator and Node RSS cannot establish this admission. The existing 512 MiB authority remains unchanged.
3. Validate one explicit native-context and immutable source-normalization policy with the real editing grids, including an original sagittal acquisition viewed axially and a memory-constrained overview. The old model crop and fixed normalization remain frozen regression inputs, not patient constants to hardcode into the application. Keep the complete real model field separate from the output crop, preserve physical aspect, and reject stale geometry before publishing.
4. Connect the verified path to the existing `useSvrSelection` history and publication authority. Hard Add/Remove voxels remain authoritative; model proposals stay drafts. Do not replace the accepted reconstruction, reset undo, recompute on hydration/navigation, publish preparation masks or silently fall back after a model error. Test literal corrections on different actual planes, including a negative-only later plane that refines a tracked prior.
5. Review the whole native/3D extent in the application, across dates and planes. Resolve the exposed lower continuation and detached upper region from source evidence and the user's intended selection; neither disputed yellow outlines nor the model's own predictions become independent truth. Validate correction quality separately from execution parity.
6. Finish real Add/Remove/Browse, cancel, undo/redo, Done, save/reopen, backup/restore, source switching, offline availability and repeated-use performance checks. Only then claim the segmentation goal complete or activate the learned path as the default. Current source-image and Node proofs remain retained evidence, not substitutes for this workflow.

## Current authority: user-disputed references, endorsed new prediction

The user reviewed the new green EfficientTAM tiny512 AX96 prediction and explicitly said it was "damn near perfect" and that the yellow ground truth was "way off." This supersedes the earlier interpretation that its upper extension was erroneous. The yellow outlines were agent-drawn engineering references, not established clinical truth, and did not represent the user's intended region adequately.

**The earlier annotation set is now USER-DISPUTED as an anatomical accuracy oracle.** Preserve its files and numerical receipts, but interpret their overlap scores as agreement with disputed labels, not proof that a prediction is inaccurate. Model rankings, rejection decisions, and proposed extra interaction requirements that depended on those labels are reopened. The mathematical scorer tests, source/geometry fingerprints, literal-mark checks, unavailable-support checks, resource measurements, and lifecycle regressions remain independently useful. Do not silently relabel any old receipt or claim that old label-dependent passes establish correctness.

The endorsed result is the unchanged, two-foreground-point EfficientTAM tiny512 AX96 output, private receipt `medsam2-pilot/outputs/e3-point-anchor-v1/receipt.json`, SHA-256 `03ae757fc9b76a3d83b938bac73b6c2dcde9a98dbf475f973115c5f3a600eb5e`. It preserves all 103 literal foreground-mark voxels without enforcement. The model uses the official Apache-2.0 code/checkpoint pair at source revision `abcd061ebd3cc6e7527d152d75b890126aaa53f6` and checkpoint SHA-256 `7d4d652a465f0081391050932f45d8a66768ccc99c8ea393ce8a5927e83f3b9b`. This is a user-endorsed positive example on one section, not yet a clinically validated or densely reviewed 3D mask.

The proposed Remove correction was canceled before rasterization or inference. The reviewed-contour interpolation prototype is paused; a more burdensome workflow cannot be justified by disagreement with an incorrect reference. Neither change has entered production.

**Unchanged propagation is now executed:** the same model, preprocessing, source, two positive prompts, and zero negative prompts produced all 63 planes of the existing native E3 processing domain using genuine forward/reverse slice memory. Both directions reproduce the approved anchor byte-for-byte. This is the complete processing domain, not the complete 221-plane acquisition. Re-audit the reference definition against the user's intended whole structure before further accuracy tuning. Do not turn the model's own predictions into independent ground truth merely to produce a passing score. Rebuilt benchmarks must distinguish user-approved regression examples, independent source annotations, and clinical labels (which are not available here).

### Current retained result and open extent question

The unchanged tracking receipt is `medsam2-pilot/outputs/e3-point-tracking-v1/receipt.json`, SHA-256 `cbb306e4511b5b4e6c7b4ea2a4580be7eaadc3580eefa24c0b99c1501a1f7e1f`. The raw result selects 40,605 native voxels, keeps all 103 explicit foreground voxels without enforcement, and selects no unsupported samples. Private CPU inference took 8.42 seconds. These are source/transport/runtime facts, not anatomical accuracy or browser-performance claims.

Root inspected label-free axial 88, 94 and 98 source overlays and the full coronal/sagittal sections. The neighboring axial sections retain the central structure and its heterogeneous texture without visible spread into the larger neighboring structures. Coronal/sagittal review leaves a real scope question open: a lower continuation reaches the processing boundary, and one separate upper component is separated by an empty plane. The user has been asked whether the lower continuation belongs in the intended selection. Do not remove it, stop propagation early, or restore the canceled Remove mark merely to agree with the disputed yellow outline. The whole 3D mask is not yet accepted.

The private user-feedback receipt `medsam2-pilot/user-feedback/anchor-endorsement.json` pins the exact shown image and approved output. It explicitly limits endorsement to the shown axial prediction. It records that the proposed Remove mark was canceled before preparation, inference, or publication.

This distinction is now enforced by the public test harness, not only by this document. Anatomical corpus admission refuses missing, disputed-engineering, user-approved-example or output-transport classifications before invoking a solver. Independent-anatomy references require a matching classification in the pinned reference file plus a pinned review-evidence file; a manifest cannot promote a disputed reference by itself. Synthetic references remain a separate category. The authority-aware scorer fingerprint includes the new classification helper, so a later authority change invalidates that fingerprint.

The separate saved-binary regression manifest (`medsam2-pilot/user-feedback/regression-v1/manifest.json`, SHA-256 `8c8c12273e0a4c854cadc3f3ad69e6f696b353cfc5c98e8f4834faee1fafb3af`) has exactly two claims: a user-approved AX96 example and complete Torch-to-web-WASM output transport. The former compares all 30,940 pixels of the reviewed section and preserves its 1,520 selected pixels; the latter compares all 1,949,220 output voxels and preserves its 40,605 selected voxels. Both have zero differences. The full-volume case explicitly has no user-approved anatomical scope. Source, support, prompts, preprocessing, implementation, model and presentation artifacts are SHA-verified. This is saved-output readback, not new inference or independent proof that a listed model produced an output; that causal evidence remains in the original linked execution receipts.

An unchanged-model E4 trial now covers all 65 planes of its actual processing domain using the separately declared full-source normalization policy. It retains all 106 original foreground marks, selects no unsupported samples, and applies no morphological filtering or hard-mark repair. Root inspected the label-free axial, coronal and sagittal outputs. The central heterogeneous body is retained, but long depth continuations, small detached components and contact with both depth crop faces leave the intended full extent unproven. The result is preserved as another reviewed model proposal, not silently accepted as independent truth. No E2/E4 disputed-reference scores were used.

### Browser-portable fidelity checkpoint

The official tiny512 model is being exported without retraining or MRI constants. CPU ONNX Runtime 1.23.2 encoder and shared prompt/decoder graphs reproduce the approved anchor exactly: 1,520 selected native pixels and zero differing pixels. Source pixels and prompts remain unchanged. This is execution parity, not a new anatomical benchmark, and is not yet a live-browser result. The graph also distinguishes an absent prior from a zero prior and checks the official multimask, stability, object-score, and object-pointer policies.

A deterministic JavaScript frame adapter now matches the exact pinned Pillow 11.3.0/Torch preprocessing byte-for-byte on 36 synthetic resize/normalization cases and seven MRI frames. An additional invalid-input test passes. It uses no canvas, display window, or labels and does not modify acquired MRI. These 44 private checks cover the model's source quantization, bicubic resampling, Float32 rounding, singleton axes, non-square frames, downsampling and enlargement. The active frontend does not yet use this model.

The actual installed `onnxruntime-web` 1.23.2 WASM backend, using the same `/all` and JSEP assets as production, also reproduces the approved anchor byte-for-byte. This runs in Node, not a browser, and does not use native ONNX Runtime as a substitute. Encoder/decoder execution measured approximately 498/34 ms in that one private run; it is not a controlled browser latency measurement.

Its memory measurement prevents an immediate admission claim. The anchor test retained 117.75 MiB of WASM linear memory and reached approximately 957 MiB process RSS. A separate cold, zero-weight identity-model control used the same runtime assets and retained 16 MiB WASM memory with approximately 248 MiB process RSS after a fixed settle interval. Thus fixed runtime overhead alone does not explain the complete anchor footprint. The remaining model-dependent compilation, optimizer and workspace contributions are not yet separated. Process RSS is not browser memory, and WASM linear memory alone is not total memory. No browser budget has been raised or bypassed, and no production model assets have been installed.

Complete sequence parity is now established for this E3 input. An explicit controller using the original PyTorch operators reproduces all 63 saved native frames and raw logits exactly. Its four-graph CPU ONNX port preserves all 1,949,220 native binary decisions, including the approved anchor and all 103 hard foreground marks. A separate JavaScript controller running the actual web-WASM backend also produces the exact complete mask, independently verified from saved bytes: SHA-256 `ad3d61b9b0f5ef87d6c5b6f640aa55a71475948ffb8adde85b8dba468f8acffb`.

Raw floating scores are not bit-identical. A strict raw-logit tolerance fails near bfloat16 memory quantization boundaries; a shared-reference-memory control reduces maximum disagreement from approximately 0.01056 to 0.000084 and passes that numerical tolerance, confirming the source of amplification. The full web run preserves all native decisions despite those score differences. Original diagnostic failures remain recorded, not hidden or mislabeled anatomical failures.

The full single-thread web-WASM run took 103.52 seconds, of which approximately 62.97 seconds were memory attention and 30.24 seconds image encoding. Its WASM heap reached 244.31 MiB and remained allocated after session release; Node peak RSS was approximately 1,120 MiB. The JavaScript controller retains at most seven bfloat16 spatial memories and sixteen object pointers, totaling 933,888 bytes; it never allocates a normalized full-frame stack. Eight synthetic controller lifecycle/packing checks and a synthetic-only raw-prior correction check pass. No canceled MRI correction was replayed.

A single four-thread Node control failed before creating the first encoder session: the installed `/all` bundle tried to `fetch` a local MJS file URL during multithreaded bootstrap. No graph or MRI prediction executed. Inspection of the actual compiled preload guard showed why neither supplying WASM bytes nor removing the MJS override would fix that path; no fetch patch, backend substitution or browser workaround was used. This is a harness/bootstrap limitation, not an algorithm failure or a browser speed measurement. Its failure receipt is preserved separately under `web-runtime-v1/tracking-full-threads4-v1/`; all one-thread inputs, graphs and outputs remain unchanged. Browser multithreaded performance is still unverified.

These measurements establish a faithful portable implementation, not an acceptable default browser experience. Live WebGPU execution, resource admission and integrated editing remain unverified. Browser admission must include model state, graph/runtime workspace, tracking memory, source context, existing editing/history and display buffers under the existing single budget authority. The new model is still a private prototype and is not the frontend's active classifier.

### Remaining integration contract

Use the existing native assembler to load one small, exclusively owned source context in the accepted patient pose, then stream model planes from that context. This avoids repeatedly decoding original sagittal files when an E1 mark was made on an axial reformat. A canonical reformat is another view of the same acquired samples, not an independent acquisition.

Extract the existing accepted-source ROI loader beneath enhancement, without inheriting enhancement's 8×-output estimate or context policy. Plan the region from native geometry and actual marks before allocating pixels. Preserve physical aspect for anisotropic in-plane samples, and keep output bounds separate from extra real input context. Never stretch a smaller output region or fabricate source padding to impersonate the approved input.

The selection hook remains the only annotation/history/publication authority. Auto-fill must not replace the accepted volume or reset undo. Native predictions projected into an overview are explicitly overview representations; the existing original-detail action remains a deliberate editing-grid change. Keep all real marks, coalesce one stroke with its completed proposal, and reject stale/canceled results. Carry the actual editing-plane metadata; off-plane marks cannot be projected into invented prompts. Multiple real conditioning planes still need a separate bounded execution proof.

Finish with actual browser/GPU latency, repeated edit/cancel/undo/retry, source switching, offline model availability, exact native/overview mapping, memory-boundary rejection and protected-cache reclamation tests. A passing private mask does not substitute for these outcomes.

The asset boundary is now concrete, but not installed: the four public-weight ONNX graphs plus two positional constants total 75,004,497 bytes (71.53 MiB). A versioned, same-origin public model directory with an app-pinned relative-path SHA/size manifest and the upstream Apache-2.0 license/attribution can use the existing Vite public-asset copy and offline ZIP path. A build-time verifier should reject missing or altered assets without downloading anything. A separately reproducible, pinned maintainer exporter must use synthetic tracing inputs and contain no private MRI paths or imports. Only the six allowlisted model files and notices belong in that directory; the surrounding private export folder contains MRI outputs and must never be copied wholesale.

The existing model Blob store can be reused, but its old one-channel, four-class BraTS manifest cannot describe this three-channel, two-dimensional binary tracker. A failed fetch, SHA check, session initialization or memory admission must leave the current marks/selection untouched and report learned selection unavailable; it must not silently invoke the old classifier. Production's current runtime entrypoint/provider/thread choices also differ from the proven single-thread `/all` WASM path and require explicit verification. None of these distribution or integration changes has been installed yet.

### Stable source normalization

The endorsed experiment's fixed range was derived from an earlier source-review crop, not a DICOM-wide window. It remains frozen for parity checks. A fresh source-only audit found that the full E3 acquisition ranges from 0 to 2,386 while the current processing/context regions range from 3 to 2,386. All 221 frames have DICOM window metadata, but there are 192 distinct window pairs: using the visible frame's window would change model inputs during traversal. The six predeclared one-cell context translations also show that crop minima can change with the crop.

Production normalization must be a separately validated, immutable source-bound choice, independent of viewport brightness, slice position and moving selection bounds. No inference or disputed labels were used to obtain the source facts above.

One subsequent, explicitly separate E3 control applies the same full-source range policy used for E4: 0–2,386 rather than the frozen 1–2,386. Geometry, source/support bytes, prompts, model, threshold and complete 63-plane processing domain remain unchanged. This changes six pixels on AX96 (two added, four removed; 1,520 → 1,518 selected) and 1,203 voxels over the full domain (832 added, 371 removed; 40,605 → 41,066 selected). All 103 foreground marks remain inside, and no unsupported sample is selected. The exact control receipt is `medsam2-pilot/outputs/e3-fullseries-policy-control-v1/receipt.json`, SHA-256 `bd70e9a62ced593d2cd1ee54ac0feb7d0d69bdc371ff4bf9932619959de900b0`.

Root inspected the new AX96 contour and an identical-source/window baseline-versus-control comparison on AX114. The anchor retains the central body's heterogeneous interior, but the largest depth difference is material: AX114 changes 262 pixels and joins areas the baseline leaves separate. The six anchor changes have small raw-logit margins; the full depth changes do not all have small margins. Neither the total changed percentage nor the apparently small one-unit range change proves equivalence. The source-wide policy remains an experiment, not an adopted replacement, and the endorsed inputs and outputs are byte-preserved. No output-driven minimum/percentile rule was introduced to force a match.

The sections below retain the investigation history. Their prior anatomical PASS/FAIL interpretations are subject to this correction wherever they rely on the disputed reference set.

## Objective and acceptance authority

The user asked to continue until segmentation works, using visually validated golden benchmarks. The intended result is a coherent editable 3D selection of the requested central structure, including its heterogeneous interior, with a small number of Add/Remove strokes. A good axial picture alone is insufficient. Source texture, geometry, explicit marks, undo, cancellation, and local-only data handling remain requirements.

The previously shown E1 AX 102 EDGE1 result is a user-endorsed positive example. The corresponding private candidate is `ce78818f3b4f8e606e5e1eb4d400123b032a0c599f43dbcace35eb773b827a50`, two-inside-stroke variant. Its image is `frontend/tmp/segmentation-accuracy/e1-candidate-two-inside.png`. That result is worth retaining as a visual target even though the replacement as a whole was not accepted.

Golden annotations in this task are visually reviewed engineering references, not expert clinical tumor labels. Ambiguous boundary tissue is marked explicitly rather than silently counted as certainly inside or outside.

## Strategy checkpoint

- **Observed failure:** previous alternatives improved E1 but lost darker interior or leaked on other dates. A fully passing synthetic suite and sparse probes failed to capture complete native boundaries.
- **Causal gap:** the appearance of painted samples is not a complete definition of object membership. The exterior image shell is also a poor model of nearby background when acquired air surrounds the anatomy. Unfiltered interior texture can dominate local path costs.
- **Retained work:** simple editing, hard marks, synchronous undo authority, safe persistence, exact native worker transport, and cancellation remain useful and do not require a particular classifier.
- **Changed procedure:** freeze independent source-only contours and explicit uncertainty across dates and planes before optimizing a candidate. Separate development E1/E3 from validation E2/E4. All dates were seen during earlier diagnosis, so the latter are held out from this tuning round, not claimed to be previously unseen patients.
- **Material alternatives:** noise-aware native-grid boundary features with competitive propagation; globally regularized hard-seeded segmentation; or a separately evaluated interactive model. Temporary guidance filtering is allowed, but acquired/displayed MRI values are immutable.
- **Forecast:** removing texture as false boundary evidence and strengthening spatial competition should retain the E1 contour while preserving heterogeneous interiors. A new source of boundary evidence may be necessary; another histogram constant alone is not a credible strategy.
- **Falsifiers:** lost explicit marks/support, loss of definite interior, new definite exterior leakage, poor cross-plane boundaries, correction instability, or unacceptable native-domain runtime. Two failed variants in the same causal area require an architectural checkpoint before further tuning.

## Benchmark design

1. Preserve the positive E1 example without treating its entire 3D candidate mask as truth.
2. Produce native-grid source-only axial sections spanning center and superior/inferior boundaries, plus coronal and sagittal sections. Use a fixed source window per examination; do not optimize the window for candidate appearance.
3. Annotate closed outlines from source images. Record source/crop/geometry fingerprints, plane and index, intended structure, uncertainty in physical millimeters, and the exact reviewed image.
4. Freeze annotations before candidate evaluation. Never generate gold from the candidate being scored. Any later correction must be justified by source evidence and versioned independently of solver output.
5. Evaluate known sections of the 3D mask, not just sparse painted voxels. Report overlap, definite-inside retention, definite-outside leakage, and physical boundary error. Unknown/unreviewed areas remain unknown, not implicit background.
6. Retain all existing hard-mark/source/geometry tests and the 22 strict replacement-candidate synthetic targets. They cannot be lowered to promote a candidate.
7. Evaluate inside-only, optional corrections, reduced marks, brush-size/position changes, and nearby disconnected distractors. Inspect native output on every material checkpoint.
8. Run held-out E2/E4 labels only against a pinned candidate. Any failed held-out result reopens the model; a later pass must disclose the resulting reuse rather than calling that data untouched.

Initial annotation convention: a 1 mm boundary-uncertainty band, with at least 97% retention of definite interior and 97% specificity of definite exterior within the independently audited local region. Confirm suitability while inspecting source-only labels, then freeze before exposing development scores. Report continuous overlap and boundary-distance metrics even when a binary gate passes.

### Reproducing the checks

Run these commands from `frontend/`. The ordinary test run exercises the public evaluator with analytic fixtures; it does not load MRI or imply a clinical accuracy pass:

```sh
npm run test -- tests/segmentationGolden.test.ts tests/segmentationGoldenCorpus.test.ts
```

The current, explicitly scoped private regression runs without inference or anatomical scoring:

```sh
MIRAVIEWER_SEGMENTATION_REGRESSION_MANIFEST=tmp/segmentation-golden/medsam2-pilot/user-feedback/regression-v1/manifest.json MIRAVIEWER_SEGMENTATION_REGRESSION_SHA=8c8c12273e0a4c854cadc3f3ad69e6f696b353cfc5c98e8f4834faee1fafb3af npm run test -- tests/segmentationRegression.test.ts
```

**The old annotations are user-disputed: their historical PASS/FAIL results do not accept or reject anatomical accuracy.** The following old entrypoint is now deliberately blocked at reference-authority admission, before any solver execution. This is a negative regression check, not a currently runnable accuracy benchmark. Old numerical comparisons and their exact old scorer hashes remain preserved in their receipts:

```sh
MIRAVIEWER_GOLDEN_CORPUS_MANIFEST=tmp/segmentation-golden/e3-derived-full-source-domain/manifest.json npm run test -- tests/segmentationGoldenCorpus.test.ts
```

The similarly quarantined original-acquisition development manifest is `tmp/segmentation-golden/development-original-native-manifest.json`. Each manifest identifies exact source/support bytes, source-grid identity, dimensions, spacing, integer phase/origin, independent marks, and frozen reference-file hashes. The solver receives only the source and marks, never the references. For appropriately admitted references, results and full masks are written beneath `tmp/segmentation-golden/portable/<scorer-hash-prefix>/<solver-hash-prefix>/<manifest-hash-prefix>/`; inspect the recorded failed cases and per-section metrics, not just the process exit status of a surrounding script. Earlier receipts remain in their original directories and are not overwritten when the scorer changes.

A standalone TypeScript replacement can be selected with `MIRAVIEWER_GOLDEN_SOLVER` and its exact `MIRAVIEWER_GOLDEN_SOLVER_SHA`. A holdout manifest additionally requires `MIRAVIEWER_GOLDEN_HOLDOUT_SOLVER_SHA` to match the already frozen solver. The 22 synthetic replacement gates use the separate explicit `MIRAVIEWER_SEGMENTATION_CANDIDATE_SOLVER_PATH` and `MIRAVIEWER_SEGMENTATION_CANDIDATE_SHA` variables with `tests/segmentationCandidateGates.test.ts`. Skipped opt-in tests are not passed accuracy gates. Learned-model experiments additionally pin preprocessing, weights, prompts, native mapping, and output receipts; their saved masks use the same public section scorer.

## Privacy and browser boundary

All MRI source images, coordinates, annotations, masks, and screenshots stay in ignored `frontend/tmp/segmentation-golden/` or the existing ignored `frontend/tmp/segmentation-accuracy/`. The resident source corpus is read-only. No Desktop rehydration, upload, or source-data commit is authorized by this task.

Do not launch, close, restart, or clean up the user's browser. Source-image visual evaluation is distinct from live UI/GPU validation. Recheck only an available supported connection when an integrated implementation is ready; an unavailable connection remains an explicit validation limitation.

## Work ledger

- Twelve development sections are frozen after separate source-image and overlay inspections: three stored-AX E1 positive references; four E3 axial sections plus coronal/sagittal sections; and original-acquisition E1 axial/coronal/sagittal sections. One millimeter boundary uncertainty is fixed before scoring. Ambiguous attachments and an unconfirmed separate focus are explicitly unscored, not mislabeled background.
- Cross-plane consistency caught eight contradictory definite voxels in draft annotations. Source-based draft corrections removed all eight before any new candidate MRI mask was evaluated. The original six frozen axial outlines were unchanged. The numerical evaluator separately tests physical uncertainty, overlapping polygon unions, explicit unknown regions, unavailable data, empty targets, exterior islands, and cross-plane contradictions.
- Ten additional E2/E4 holdout sections are frozen after independent source-only tracing, root inspection of every final overlay, and zero definite cross-plane conflicts. No E2/E4 candidate masks or scores have been exposed to the annotator or tuning lane. Together with development data, the reference now contains 22 reviewed sections across four examinations and three planes, on two distinct source-grid families.
- The workflow audit found a material benchmark gap: E1's default original-3D path selects its original sagittal acquisition, not the stored axial reformat used in the earlier corpus. Cold/constrained-memory overviews also change the source grid and sample phase. The golden source deck now uses actual `planNativeVolume` plus `assembleNativeVolume` for the original acquisition. Numerical evaluation must compute mark-centered bounds from the full source plan, then copy that exact domain; an arbitrary annotation crop cannot define the solver's artificial exterior.
- Seven new classifier/planner/assembly/brush/hook workflow tests passed. They protect actual source selection, sample/support mapping, planar brush semantics, hard marks, coalesced undo, and original-detail transfer as a draft, without treating old masks as accuracy truth.
- The first graph-cut prototype passed 18/22 fixed synthetic quality gates; the second passed 20/22, including all bright/dark/weak/heterogeneous cases and the previously lost common-gray interior. The two remaining failures were an air-surrounded halo and global expansion after an additional edge mark. Numerical min-cut implementation was independently checked against 100 exhaustive tiny-graph optima; optimizer correctness does not establish anatomy correctness.
- Gradient-based exemplar confidence exposed another authority error: treating unreliable appearance evidence as a weaker foreground likelihood implicitly adds negative evidence. Further prototypes remain private and rejected unless all gates pass. Confidence belongs in deciding how much to trust appearance versus spatial evidence, not in overriding a hard mark.
- A diagnostic confirmed that acquired air can dominate the unlabeled background histogram and suppress the likelihood of ordinary tissue. This is a mixture-prevalence problem, not evidence that distant air should control the local boundary. Any correction will be tested independently, with no patient-specific constants.
- **Strategy checkpoint:** native development evaluation starts with the strongest pinned experimental candidate while synthetic work continues. Requiring every synthetic proxy to pass before inspecting actual MRI would repeat the previous delayed-feedback failure. This is an experiment, not permission to ship a candidate with failing gates. E2/E4 remain reserved until model selection.
- The v2 native experiment improved E1 stored AX98/100/102 to 98.26–99.50% confidence-region Dice, with boundary p95 1.16–1.96 mm. Root inspection confirmed reduced exterior halo but residual junction underselection. The same pinned model failed every E3 development section in both inside-only and corrected workflows: surrounding tissue remained selected. Hard marks, source immutability, support, and connected-component checks passed, so this is a model failure rather than a transport or publication error.
- **Architecture checkpoint:** global painted-color likelihood plus distance to a far exterior does not establish local object membership. Unmarked context contains both target and background; normalizing every context mode as background fixed an air-mixture example while suppressing valid target interiors. The next proof must give local boundary or region evidence authority instead of globally publishing the colors of brush marks. The optimizer and lifecycle primitives remain reusable; none of the private v1–v6 classifiers is accepted. Original-acquisition and memory-constrained source-grid validation remains required before integration.
- Native original-acquisition evaluation exposed a separate production defect in `markedRegionBounds`: adding Remove marks that already fit the foreground search region recomputed a wider, shallower box, clipping valid tissue on another axis. The corrected domain could not reach 97% recall even with a perfect classifier. The planner now keeps the foreground-defined context when it already contains every outside mark; genuinely outlying marks still receive a budget-bounded combined region. A geometry regression and worker-reuse regression failed before the fix; 71 focused core/worker/lifecycle checks passed afterward.
- Revalidation with unchanged original MRI bytes and frozen references confirmed the domain fix: current production segmentation now passes all three original-E1 corrected section gates, as does the unchanged v2 experimental classifier. Root directly inspected the new sagittal/coronal production masks and the old coronal failure. The old result omitted most of the large lower component; the new one retains it. This is an accepted search-domain correction, not acceptance of a replacement classifier across dates.
- A separate active-cancellation defect allowed abandoned scratch/CPU work to survive in a reusable worker. Cancelling an active operation now rejects it, releases its listeners/timers, and terminates that worker; the next operation recreates it. A completed idle worker still reuses its exact source crop. Seventy-four focused core/worker/lifecycle checks passed with this change. This is lifecycle correctness, not segmentation-accuracy evidence.
- Further architectural proofs remain rejected: boundary-only random-walker diffusion passed independent dense-system and hard-mark checks but missed much of the synthetic interior and exceeded its 30-second native E3 budget without publishing a mask. A rooted local-appearance graph cut avoided E3 leakage mainly by selecting very little of the target; all 12 E3 section/gesture outcomes failed. Shape and appearance are being ablated independently rather than attributing this combined failure to a guessed geometric cause.
- **New bounded alternative:** evaluate a pretrained promptable biomedical model locally, using the same independent marks and frozen development references. [ScribblePrompt's official implementation](https://github.com/halleewong/ScribblePrompt) offers a compact grayscale UNet with scribble inputs; its published results are not evidence that it works on this corpus. First prove useful segmentation on the marked native E1/E3 sections before attempting 3D propagation, native-boundary refinement, browser integration, or weight distribution. No MRI is sent to a hosted demo or service; only public model code/weights may be downloaded into ignored local research files. Source MRI/display values and explicit marks remain authoritative. If the model cannot recover the source-defined body with the existing simple gestures, reject it without adding a permanent dependency.
- Native development/held-out acceptance: pending.
- Production integration and whole-workflow regression: pending.

## Learned-model development checkpoint

The local-only pilot uses the public ScribblePrompt-UNet v1 checkpoint, pinned to SHA-256 `43f57ee8fa8ec529c31be281e06749f9e629b30157bbbcc9baf200cddec1acbe`, with the official architecture at upstream revision `182c44975f77749b559974ce8db558c8bde57788`. The 3,991,882-parameter model consumes source MRI and independent positive/negative marks; it does not read golden polygons. Source pixels, display windows, marks, and the 0.5 probability threshold are fixed. Code, weights, numerical outputs, and private reference images remain under ignored research paths. There is no frontend model dependency or deployed classifier change at this point.

The native marked-plane results below are confidence-region precision / recall percentages. Each row must satisfy both unchanged 97% gates; passing a row does not establish 3D fidelity. These are engineering-reference comparisons, not clinical validation.

| Fixed experiment                                          | E1 inside-only | E1 corrected  | E3 inside-only | E3 corrected  |
| --------------------------------------------------------- | -------------- | ------------- | -------------- | ------------- |
| Single prediction, corrections re-predicted independently | 100 / 77.60    | 100 / 97.67   | 99.54 / 96.47  | 87.38 / 97.79 |
| Documented iterative interface using prior logits         | unchanged      | 99.89 / 97.01 | unchanged      | 91.89 / 96.36 |
| Eight exact orientation symmetries, averaged posterior    | 100 / 91.44    | 99.93 / 99.09 | 93.47 / 96.36  | 87.04 / 96.36 |
| Iterative prediction plus native-edge Potts refinement    | 100 / 77.56    | 99.89 / 97.01 | 99.54 / 96.36  | 91.81 / 96.47 |

The E1 corrected rows pass; none of the four procedures passes all marked sections. Direct original-resolution inspection confirmed coherent central objects, but repeated E3 errors include an upper dark-tissue cap and a lower-right omission. Orientation averaging is not a general improvement. The fixed native-edge refinement changed only four pixels in corrected E3: a numerically correct optimizer cannot repair a confidently wrong spatial prior merely by adding this edge term. Its 100 exhaustive tiny-grid energy checks passed, independently of the failed anatomy result.

CPU measurements for the unbatched single-plane model were approximately 37–111 ms per forward pass with two Torch CPU threads; the eight-orientation experiment took approximately 0.73–1.16 seconds per plane. These are private Python/CPU measurements, not browser latency, GPU performance, or a completed 3D selection time.

### Orthogonal predictions do not establish shared 3D object identity

The sparse orthogonal proof is complete and rejected. It inferred only sections genuinely intersecting the independent foreground marks, maintained physical aspect ratio, and left unobserved voxels unknown. No marks were copied onto unmarked slices. The 46 marked E3 planes took approximately three seconds of two-thread CPU inference. A fixed native 3D cut preserved hard marks and source/support but failed all 12 section/gesture outcomes. Corrected coronal recall was 49.06%; axial 96 recall was 86.87%. Small unseeded islands also remained. The cut used approximately 156.5 MiB of typed scratch, excluding neural inference and input residency.

A separately source-reviewed Add correction on the missed coronal continuation tested the interaction rather than adding a reference-derived prompt. It added nine actual marked voxels. The corrected single-plane model improved coronal recall from 50.94% to 92.81%, still below the fixed gate. Applying the same 3D fusion reduced that useful result to 75.31% recall; sagittal recall fell from 51.16% to 43.93%. Direct source-image inspections confirmed interior gaps. This is a structural failure: independently predicted planes disagree about which 3D object the user means. More threshold tuning or edge smoothing would not supply that missing identity. The corrective input remains a post-proposal interaction benchmark, not initial or held-out accuracy evidence.

A different, fixed native minimax-boundary propagation experiment also failed. Independent exhaustive path and foreground-monotonicity checks passed, but only 2/22 synthetic quality gates passed. Native E3 passed none of 12 section/gesture outcomes, with axial 96 recall 31.24%, coronal recall 10.31%, and sagittal recall 2.02%. Both mark configurations produced the same fragmented selection. This method is rejected without further tuning.

### Full-source E3 control

The actual production-planned E3 domain is 182 × 170 × 63 native voxels, not the earlier 187 × 187 × 51 diagnostic crop. All 1,949,220 assembled samples matched an independent full-source pixel oracle, and all 1,577,940 overlapping diagnostic samples matched exactly. The original 103 foreground voxels are unchanged; every definite-inside reference voxel is reachable.

The current production baseline took approximately 1.285 seconds and retained all definite interior, but failed all six section precision gates (23.44–37.21%). Its selected volume grew by 64.35% relative to the arbitrary diagnostic-domain baseline, and shared-region mask Jaccard was 0.556. Both masks stayed within the earlier reviewed extent, so the difference is a domain-dependent classifier change, not merely extra displayed canvas. Root inspected the native coronal result and confirmed broad surrounding-tissue spill. This falsifies an accuracy claim based only on the smaller crop.

Modeled overview sampling remains a separate limitation: cold-cache E3 uses source strides [2, 1, 1], while the 256 MiB-cache case uses [3, 2, 1]. All four reference axial planes are present, but their in-plane samples differ. The cold overview omits the exact sagittal reference plane; the constrained overview omits both the exact coronal and sagittal reference planes. No native reference was silently treated as a reviewed outline for a neighboring plane.

### Architecture proof: one promptable 3D model

**Evaluation protocol correction:** the initial FastSAM and full-teacher calls below used `masks=None`. Re-reading the pinned upstream 3D validation path showed that it instead initializes a zero-valued 32-cubed mask tensor and passes that tensor even on the first click. Those two paths use different learned components. The initial negative images and scores remain genuine records of the calls executed, but they do not establish failure of the documented pretrained-model procedure. Conclusions about either model's accuracy, or a need to change its inference scale, are reopened pending a corrected zero-mask run. Source geometry, references, checkpoint identity, and measured resource usage remain independently verified. The mistake was in this evaluation adapter, not the MRI data or the production application.

The current bounded experiment uses [the pinned FastSAM3D implementation](https://github.com/arcadelab/FastSAM3D/tree/226dfb5cdc80b7d5dbd3ab023c666d98ff3d7175), which processes one spatial volume before interpreting user points. Its author-linked Apache-2.0 checkpoint is pinned to SHA-256 `14319b3d3a208a00fcc5b8b6ada1af978a81f6485312e368db39eec2507f6022`. Published results do not establish accuracy here. The released checkpoint contains its own fine-tuned encoder, prompt encoder, and decoder; mixing in a different teacher decoder would change the model and is not the intended proof.

The source-only input contract follows the documented 1.5 mm resampling, canonical RAS orientation without deobliquing, 128-cubed context, and positive-voxel Z-normalization. The pilot substitutes a foreground-mark-centered crop for the upstream benchmark's ground-truth-centered crop. Full acquired MRI supplies the context; zeros are allowed only outside actual acquisition support. Source/display MRI remains immutable. The fixed model result must be mapped through the recorded patient affine back to the actual native selection domain before scoring. Model inputs may not read golden references.

The first full-volume E3 **no-mask control** is complete and fails all six sections, both raw and after hard-mark enforcement. It uses two actual foreground-component medoids, with no background prompts or previous mask. The raw model selects 274,524 voxels and misses 42 of 103 explicit foreground voxels. Enforcing marks adds exactly those 42 voxels; it does not fix the broad neighboring-tissue selection seen in the axial and coronal source images. Axial 96 raw precision/recall is 7.42%/85.65%; coronal precision/recall is 11.73%/100%. There are also 589 unseeded island voxels. This executed control predicts the wrong region, but its first-mask contract is not the documented procedure.

All 216 checkpoint tensors strictly match the released architecture, with 203.04 MiB of retained float32 state. The private CPU run measured approximately 60 ms for encoding and 19 ms for decoding, plus 2.28 seconds for file verification/loading; its Python/Torch process peak was approximately 688 MiB. These numbers neither certify browser feasibility nor make the failed segmentation acceptable. An integer-coordinate serialization bug in the private export adapter caused the first inference attempt's output to be lost; the corrected adapter validates exact-integral coordinates before inference and durably saves raw model output before mapping/publication, allowing an inference-free resume.

The fixed interaction check uses the same image embedding and existing source-only marks in their original first-occurrence order: first Add, second Add, then the four previously recorded Remove gestures. Each later decoder call receives only the newest point and the preceding raw low-resolution logits. The first version still inherited the incorrect no-mask initialization. It satisfied all 103 inside and 84 outside marks after the last correction, without enforcement, but failed all six anatomical sections: coronal precision 32.65%, axial 96 precision 51.45%, sagittal recall 88.44%, and axial 88 recall 32.47%. Its coherent but oversized central selection is visible in the saved source images. These results describe that incorrect initialization only.

The corrected protocol checks reused the already saved embeddings: a zero 32-cubed first mask, then the same source-only point sequence and raw prior logits. A hook on the actual mask encoder verified the initial zero tensor and every subsequent raw-logit handoff. No encoder replay, new annotation, model weight, threshold, or crop was used. Initial two-Add and final corrected accuracy remain separate outcomes; optional negative marks cannot silently influence the initial case.

Both models still fail all six E3 sections under the corrected six-gesture procedure. These results supersede the no-mask controls for this particular inference contract; they do not claim to reject every possible interactive use of either model.

| Correct zero-prior sequence, after two Add and four Remove gestures | Selected native voxels | Coronal precision / recall | Axial 96 precision / recall | Sagittal precision / recall |
| ------------------------------------------------------------------- | ---------------------- | -------------------------- | --------------------------- | --------------------------- |
| FastSAM3D, raw and enforced identical                               | 36,272                 | 36.99 / 100%               | 54.91 / 100%                | 65.27 / 85.84%              |
| Full teacher, raw                                                   | 43,682                 | 39.72 / 95.94%             | 36.44 / 100%                | 41.82 / 78.32%              |

FastSAM3D retained all 103 inside and 84 outside marks without enforcement. The teacher retained every inside mark but selected 32 outside-mark voxels; literal enforcement removes those 32 and does not repair the anatomical error. Both final raw masks contain one seeded component and no orphan islands. Root inspected the final coronal source overlays: the Fast result retains a broad neighboring-tissue halo; the teacher also misses the lower tip. The axial 88 section remains substantially wrong (Fast recall 20.78%; teacher recall zero). Neither is accepted.

The six cached Fast decodes, including one verified model load, took 4.23 seconds; the teacher control took 4.46 seconds. Host load varied across runs, so these private CPU measurements are not a controlled performance comparison or browser latency. The teacher's retained float32 model alone is approximately 383 MiB, and its unmodified local-attention allocation is not admitted under the current browser memory budget.

**Next representation test:** build the same 128-cubed input at 0.75 mm from the original full MRI, with the same foreground-only centering and normalization. This is not an upscale of the old model image. It doubles spatial sampling while reducing physical context to 96 mm. All 1,949,220 voxels of the unchanged native evaluation domain and all original marks remain within the supported input, so a narrower evaluation box cannot manufacture a precision improvement. Root inspected the new source-only input before authorizing the same fixed six-step Fast sequence. The input is pinned to `48aea24fabfa96b6b4dd7de6b3c373575002c75537a916c7ec74db11af512056`.

In parallel, one fixed proof reuses the existing native 3D edge energy with the complete coherent Fast probability field, rather than the earlier disagreeing sparse 2D predictions. This tests recognition and native boundary fitting separately. It adds no fabricated labels, threshold search, reduced evaluation domain, or production dependency. A remaining definite halo or missing continuation rejects the proof; mathematically correct optimization alone is insufficient.

Both bounded proofs are now rejected. At 0.75 mm, the two-Add proposal contains 15,795 native voxels and still fails all six sections. Coronal precision improves to 87.79%, but recall falls to 71.88%; axial 88 recall is zero. After the four existing Remove gestures, the model selects 7,839 voxels: axial 96 precision/recall is 98.71%/84.22%, coronal 95.38%/70.94%, and sagittal 97.13%/58.67%. All marks, support, and seed connectivity hold, but clear interior is missing. Root inspected the two-Add coronal and final axial outputs. One new encoder plus six decodes took 3.95 seconds including verification/loading, in private CPU inference only.

The unchanged native edge refinement on the coherent 1.5 mm final prior selects 36,092 voxels versus 36,272 before refinement. Its 1.26-second core calculation uses approximately 179.4 MB of explicit typed allocations, excluding approximately 31.2 MB of driver inputs and runtime overhead. Root inspected the result: it leaves the oversized dark exterior ring and adds small contour irregularities. This specific spatial-prior-plus-edge energy is not an accepted boundary solution; no coefficient sweep or finer-context rerun follows it.

The focused sequence exposed an independent interaction defect. Every gesture after the first was already fully satisfied by the preceding raw mask: all 61 voxels of the second Add were inside, and all 21 voxels of each Remove were already outside. Nevertheless, those five reruns reduced the selection from 19,194 to 7,839 voxels. This does not establish that the first proposal was accurate; it establishes that redundant confirmation can substantially alter an existing proposal. The production hook currently schedules automatic recomputation even for a mark-only edit with an empty membership patch. The correction under implementation retains such marks and their reversible history without rerunning a known completed proposal. Initial, pending, failed, and genuinely corrective work must still run; explicit retry remains available.

The separate recovery benchmark applied the already source-reviewed nine-voxel coronal Add correction to the focused final proposal, retaining all earlier inside/outside marks. Six of these nine source-defined voxels contradicted the prior mask. One cached decoder call improved coronal recall from 70.94% to 82.50% raw, or 83.44% after explicitly restoring three missed new marks. Axial 96 recall reached 87.42%, sagittal recall 67.92%, and axial 88 remained zero. All six sections still fail. Root inspected the enforced coronal result and confirmed that the lower tip remains missing. Useful local response is not acceptance of the method.

**Architecture checkpoint:** these point-prompted 3D procedures do not preserve enough of the intended structure, even with corrected initialization, finer source sampling, and a meaningful orthogonal correction. The fixed native edge energy also fails to resolve their boundary error. Keep their measurements as rejected experiments and retain the independent runtime fixes; do not add more threshold/crop variants or a permanent model dependency.

The next bounded alternative uses [MultiverSeg's context-conditioned model](https://github.com/halleewong/MultiverSeg/tree/f351a0cdb3d6222b4d9b6055a06aba84f8e8c2b6) to interpret neighboring acquired slices using the same initial source slice and its inferred mask. The first mask comes from the already tested ScribblePrompt model with actual marks, not a reference polygon or an expert label. Thus it remains an imperfect algorithmic prior. Unlike the rejected independent orthogonal predictions, every target slice receives the same explicit source/mask context. No new hard marks are projected onto unmarked anatomy. The first proof must generate the complete unchanged native output volume and pass the same cross-plane evaluation; an attractive anchor slice or successful model load is insufficient. If context propagates the wrong boundary, loses ends, or causes inconsistent sections, that falsifies this procedure. Public models and private MRI remain local research artifacts until accuracy, resource admission, and the editing workflow are demonstrated.

The falsifier is unchanged: if the model cannot produce a coherent whole target and respond usefully to a small number of real corrections, it is not accepted. Initial and corrected results must be reported separately. Hard marks, source/support, connectivity, held-out performance, memory admission, and actual editing UX remain required. The 32-cubed decoder logits also make native-boundary detail an explicit validation concern, not a promised benefit of using a neural model.

There is no production model dependency yet. Local CPU feasibility is not browser feasibility: the installed web runtime lacks the FastSAM3D decoder's rank-five transposed-convolution operation, and an exact lowering or measured compatible execution path would be required for that model. Model weights, scratch, caches, transfers, and retained editing/display buffers must fit the same admission authority; increasing the memory cap or silently reducing scientific detail is not an accepted shortcut.

### Shared-context stack result and next decision

The fixed MultiverSeg proof is complete and fails all six E3 sections. It runs one ScribblePrompt anchor at AX96 and 62 queries against that single immutable inferred context, covering all 63 native planes. Raw and hard-enforced masks are byte-identical: 47,062 selected voxels, all 103 foreground marks retained, and no unavailable samples selected. However, 28,637 selected voxels belong to components without a foreground mark. This is not an acceptable 3D selection.

| Native section | Confident precision | Confident recall |
| -------------- | ------------------- | ---------------- |
| AX94           | 100%                | 86.37%           |
| AX96           | 99.76%              | 91.83%           |
| AX98           | 100%                | 82.04%           |
| AX88           | 100%                | 88.74%           |
| COR245         | 40.51%              | 70.00%           |
| SAG257         | 37.12%              | 92.49%           |

Root directly inspected AX88, AX96, COR245, and SAG257. Axial sections locate the intended body but undershoot its rim; coronal and sagittal views expose disconnected patches and long exterior extensions. The complete private CPU run took 13.00 seconds, including 11.86 seconds of model queries, with 1.37 GB process peak RSS. Neither this resource measurement nor the source overlays establishes browser feasibility or live UX correctness.

**Strategy reset:** a common support example is not the same as persistent object tracking. The failed observable outcome is a coherent whole structure, even when the marked axial section looks plausible. Keep the source/mark/lifecycle authority and frozen evaluator; replace the assumption that independently querying every plane supplies longitudinal identity. Before another model run, a bounded saved-probability diagnostic will test whether ordinary seed-connected filtering and one global threshold can recover the structure. It is development-only analysis, not permission for per-section tuning or reference-derived input. A genuinely memory-based, bidirectional slice-tracking model is being audited as a materially different alternative. Its first proof must use the same imperfect inferred anchor, actual source coverage, and unchanged marks; it must not receive a golden contour. No candidate is accepted or integrated on this evidence.

That diagnostic is complete. None of the nine fixed thresholds (0.1 through 0.9), either raw or retaining only six-connected components attached to actual foreground marks, passes all six sections. At 0.5, filtering removes all 28,637 orphan voxels but leaves coronal precision/recall at 63.82%/70.00% and sagittal at 67.94%/92.49%. The best tested connected result still has a weakest precision/recall of only 66.56%. Exact per-section raw probability-rank bounds independently show that no raw threshold can satisfy the gates: even the best precision/recall balance is bounded by 62.19% on COR245 and 72.13% on SAG257. This bound does not claim to exhaust every possible connected-threshold result. All 103 hard foreground marks survive all tested thresholds before enforcement; no masks were changed or published, no inference was repeated, and the 0.5 reproduction is byte-identical. This closes ordinary thresholding/filtering as the immediate remedy for this output.

### Evaluator correction and preserved evidence

Independent review found three benchmark defects: a positive reference with no confident acquired interior could pass vacuously; reference-file schema/split was not tied strictly to corpus admission; and boundary distances could use unknown or coverage-clipping edges as anatomical targets. The portable runner also failed to project in-domain unavailable/nonfinite samples into the section's source support.

These are corrected without changing the 97% gates, 1 mm positional uncertainty, annotations, or masks. Non-informative references fail explicitly, including empty negative controls without reviewed acquired exterior. Every reference's schema and split are checked before candidate execution. Boundary measurement clips both contour sets to known acquired source without inventing closing edges. Unavailable samples inside the processing domain are unknown; acquired reference anatomy outside that domain still counts as unreachable false negatives. One scorer hash covers the overlap, geometry, admission, and topology code.

Eighteen new regressions failed before the fixes; 51 synthetic evaluator tests passed, with the two private gates deliberately skipped in the ordinary run. At that checkpoint the scorer revision was `0c019afd53d3fcdca71f67df7b51aa28fac66f2aacffc6d5d0f0ac826aa426c0`. A saved-mask control recheck, with no new inference, confirmed that all nine baseline references were informative and all overlap/count results were unchanged: original-E1 corrected remained 3/3 passing, while the actual-domain E3 baseline remained 0/6. These historical label-dependent results are subject to the user-disputed-reference correction above. Source/support, masks, hard marks, and topology were unchanged. Some boundary-distance diagnostics changed where semantic unknown regions clipped the contours; old distances were not silently carried forward as current evidence. Old receipts remain immutable. E2/E4 were not evaluated during that tuning round.

## Additional accepted reliability work

Native-detail admission now obtains live retained-editing memory at the actual request boundary, releases only an idle selection worker, includes retained alignment buffers and distinct selection buffers, and relies on the native loader's single planner. ROI/settings commit only after successful refinement. Failure retains the accepted volume, marks, history, and settings. Eighty-five focused tests passed, including rejection before frame reads without reducing the native grid.

Completed display enhancement previously disappeared when a native-detail request merely became busy, even if that request then failed. Its lifetime is now tied to source/selection identity rather than the transient busy flag. Admission counts deduplicated retained CPU buffers and the actual enhancement texture reservations; rejected requests preserve the completed enhancement and comparison state. One hundred thirty-five focused viewer/selection/enhancement tests passed. These are bounded lifecycle/memory proofs, not browser RSS, accuracy, or final whole-worktree validation.

Selection publication and undo composition no longer accumulate a JavaScript list for every changed voxel or allocate oversized combined patches before copying them again. Full-volume patches use counted, exact-sized typed arrays. Composition skips sort scratch for already ordered inputs and allocates only the resulting reversible edit. Sparse brush order and the previous duplicate/reversal semantics are preserved. Eleven focused tests include 750 deterministic equivalence/history cases and a 120,000-voxel complete reversal. Forty-two additional hook, real workflow, and cancellation/lifecycle tests passed against this implementation.

In a Node-only 600,000-entry composition example, explicit typed allocations fell from 8.7 MB to 2.7 MB; complete reversion required no new typed output. Dense-edit microbenchmarks improved substantially, while scanning a million-voxel mask with only 1% changes cost approximately 0.8 ms more because of the second pass. These are allocation accounting and function-level timings, not browser heap measurements or end-to-end MRI performance claims.

Confirming a completed selection no longer schedules another automatic proposal. The existing synchronous history owns its completed-proposal diagnostics; a mark-only Add or Remove preserves that completion, records the new hard marks and reversible draft edit, and retains boundary/context warnings. Initial, unfinished, failed, changed-membership, restored, or source-replaced state still requires computation. Explicit retry remains a force request. Three new scheduling regressions failed before this correction.

Mark-only strokes also retain the exact selection data buffer rather than cloning identical bytes. Empty-patch undo/redo returns that same buffer. This prevents a metadata-only confirmation from invalidating the display enhancement, whose lifetime correctly follows source/mask identity. Two hook identity regressions and both empty-history identity regressions failed before their fixes. A joint test now drives the real selection and enhancement hooks together: confirming Add/Remove and mark-only undo/redo preserve the completed enhancement, strength, and retained-memory accounting without another worker call; changing actual membership invalidates it. These are automated lifecycle checks, not live GPU or MRI-accuracy evidence.

The supported browser connection was checked again and returned no available browser. Read-only official diagnostics show Chrome running with its extension enabled and a valid native-host manifest, but the browser connection still cannot be discovered. The user has been asked for permission to open one temporary blank Chrome window through the supported recovery flow; no such window has been opened while permission is pending. No browser or profile was restarted, closed, or accessed through an alternate workaround. The existing localhost server still returns HTTP 200 with cross-origin isolation headers. Source-image review continues; live UI/GPU validation is a separate outstanding check.

Native frame streaming now reuses already decoded Cornerstone cache entries without inserting new processing frames. The existing bounded native-frame cache remains the owner of its converted pixel/validity buffers; ordinary alignment and 2D image callers retain their previous caching behavior. Four new regressions failed before the change; 46 focused decoder/native-frame/abort tests pass afterward, including an actual Cornerstone cache-size/readback test, source immutability, padding, VOI and physical geometry.

A second real cleanup defect was found in the outer DICOM loader. The installed WADO loader caches parsed datasets by its parsed URL (for example `0`), but the application tried to unload the complete image ID (`dicomfile:0`). Cleanup now uses the dependency's own parser and the correct key. Two regressions using the actual installed reference-counted dataset cache reproduced retained entries before the fix and pass afterward: twenty sequential loads return cache residency to its starting value while retaining decoded image bytes, and a decoder failure releases its parsed dataset/Blob as well. An independent cached dataset is preserved. Forty-nine focused source-loading/cache tests pass. This is synthetic ownership proof, not a claim that the user's live browser memory was measured or purged.

Memory admission now measures the distinct typed-buffer allocations retained by cached **and displayed** Cornerstone images, including parsed DICOM byte arrays and known auxiliary pixel/LUT arrays. Shared backing allocations are counted once; an image still displayed after cache removal remains counted. Missing or opaque telemetry retains a conservative reserve. A small pixel view into a much larger parsed allocation cannot incorrectly consume that reserve as if the entire allocation were logical cached pixels. Native planning, enhancement admission and custom-model preflight share this measurement; preflight remeasures at the actual request boundary. This is known CPU-buffer accounting, not a measurement of arbitrary parser objects, runtime scratch, process RSS or GPU-driver allocations. The existing separate reservations remain necessary.

Independent-2D reconstruction now also reuses existing Cornerstone entries without inserting processing frames. The converted source/support arrays remain the reconstruction's owner. At most one current image and four prefetched images overlap decoding, with series processed sequentially. Source admission accounts for that bounded batch separately from the later reconstruction peak instead of forecasting a retained parsed dataset for every frame in a long stack. The existing conversion/padding/crop reservation is not charged a second time. Existing Blob-size metadata supplies the source-file forecast without reading Blob contents or changing the database schema; unknown sizes keep a conservative decoding reserve. A Blob's size is not a bound on arbitrary parser inflation.

The source-memory change passed 165 focused tests across seven suites, including actual Cornerstone cache behavior, displayed-but-uncached owners, buffer aliasing, successful/failed/canceled loading, source/support immutability and bounded prefetch memory for long stacks. The 702-frame synthetic fixture retains the **requested** 1.00 mm setting with an empty cache, while its 256 MiB already-resident case still requires the existing 1.19 mm memory adjustment. The independent volume-dimension limit can make effective output spacing coarser: this test does not prove a 1.00 mm output grid or a measured fidelity improvement.

The optional full-MRI enhancement-admission fixture now exposes its actual decoded-image owners and displayed-image callback to that same accounting path. Its thresholds, region sizes, geometry and admission gates are unchanged. A new tiny synthetic ownership check passes. The full MRI case remains skipped: its required original ordinal-18 acquisition is not verified resident in the protected four-study copy. No different study was substituted and no Desktop/FileProvider hydration was attempted. This fixture models decoded pixels, not retained parsed DICOM datasets, anatomical truth or browser memory.

### Scrolling follow-up: reuse accepted alignment before analysis

The user reported that comparison panels lag behind the reference while browsing. The viewer correctly retains its previous aligned image until a replacement is ready, but even an already accepted registration waited for reference capture, similarity/display analysis, and sometimes a different scan's cold registration before publishing that replacement. This was unnecessary work on the presentation path, not a reason to show an unregistered native slice.

Accepted registrations now retain their verified source geometry and frozen display calibration. Browsing first reuses an exact cached plane or resamples the requested plane using the accepted full 3D pose, affine transform, brightness/contrast mapping and manual slice offset. This path performs no reference-pixel capture, pose refinement, similarity scoring or per-slice tone fitting. Current reference presentation adjustments are composed with the accepted calibration. Geometry-only preparation allocates no reference pixel buffers. Newly resampled presentation frames are explicitly unscored; they are not new registration-quality evidence.

Cached revisits publish first, followed by other reusable registrations, before the existing debounce and analysis for cold scans. Uncached planes still require normal source loading and resampling; this change does not promise zero latency or frame-perfect lockstep. Large source slabs remain bounded and serial. Aborted source consumers stop waiting without canceling shared image loads. Request ownership, source identity, output geometry, support-buffer lengths and the unchanged accepted pose are checked before publication. A late result cannot rewind newer browsing or resurrect an explicitly cleared registration. Successful reuse also refreshes the existing bounded model cache's recency.

The regression coverage includes blocked cold scans, real wheel events, rapid forward/backward browsing, manual offsets, affine/tone preservation, cached buffer identity, cancellation, malformed reslice results, cache eviction and zero-contrast fallback calibration. Several of these checks were demonstrated failing before their respective fixes. The integrated wheel test drives the real viewer/alignment/settings hooks with small synthetic volumes and verifies pixels delivered to Cornerstone while a cold capture remains unresolved. It is synthetic presentation-path evidence, **not** an MRI visual review, a GPU rendering test or a measured browser-latency improvement.

The existing preview and the new browsing module both return HTTP 200 at `http://localhost:43124/`. The supported browser connection remains unavailable; no browser was restarted, closed or accessed through a workaround. Live visual validation of scrolling remains open. This follow-up does not change the preserved approved segmentation output, admit the model prototype into the frontend, or resolve the outstanding 3D anatomical review.

### Current worktree validation

After the source-memory changes, fixture corrections and scrolling follow-up, the complete ordinary suite passes **2,069 tests**, with **54 optional tests skipped**, in 124.37 seconds. There are 155 passing test files and six entirely skipped files. The subsequent fixture-only addition of truthful geometry metadata also passes the affected source/wait suites: 42 tests. Skipped private corpus/model checks are not accuracy passes. The previously run separate pinned saved-output regression and evaluator suites pass 90 tests with two optional cases skipped; the approved AX96 mask and complete Torch-to-web-WASM binary transport remain unchanged. The previously run deterministic frame/controller checks pass all 52 private cases without new MRI inference; no segmentation inference or private output was changed during this scrolling follow-up.

The current TypeScript/Vite production build, whole-tree lint, and React Doctor run also pass. A separate TypeScript check of the application and all five changed browsing test files reports zero diagnostics. Doctor scans 352 files with scoring, telemetry and supply-chain checking disabled; it reports no issues, not a numerical score. Existing React `act` warnings in viewer tests and Vite's large-chunk warning remain visible. ESLint ignores the already Git-ignored private `tmp` experiments, consistently with the repository's existing formatting/Doctor exclusions; no production rule was disabled.

`git diff --check` passes. Nothing is staged, and no files under the protected MRI source directory or `frontend/tmp/` are tracked. New source images, model weights, masks and execution receipts remain private ignored artifacts. Existing unrelated worktree changes and older visual artifacts were preserved, not staged or cleaned up. These are checks of the current dirty worktree, not a committed-head, CI, deployment, clinical-accuracy or live-browser signoff. Model integration and the full 3D extent remain open as described above.

## Technical reference

The graph-cut prototype follows the general hard-seed/soft-region-and-boundary formulation of [Boykov and Jolly, ICCV 2001](https://cs.uwaterloo.ca/~yboykov/Abstracts/iccv01-abs.html). Guidance filtering, statistical normalization, and physical weighting are this application's hypotheses, not properties validated by that paper for this corpus. Native MRI bytes and display texture remain unchanged.
