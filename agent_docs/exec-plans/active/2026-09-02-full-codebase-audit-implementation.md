# Audit implementation and CTO-review remediation

**Current task:** finish the [September 1 full-codebase audit](2026-09-01-full-codebase-audit.md). The [September 3 CTO review](2026-09-03-cto-architectural-review.md), except PR splitting, shipped in [PR #11](https://github.com/blader/MiraViewer/pull/11), merged as `e84e208264431164eeeb627c544fc38e62db1155`. Its automated acceptance passed. Raster/aesthetic sign-off is withheld because the image-preview path was unreliable; independent pixel, geometry and interaction checks passed. That merge does **not** close the larger audit.

This file is the current implementation/status authority. The [architecture guide](../../design-docs/miraviewer-architecture.md) explains the live design. [Historical checkpoints](../historical/2026-09-02-full-codebase-audit-checkpoints.md) and older plans preserve their evidence and unfinished proposals, not current completion claims.

## Contract

Preserve local-only image processing, source and patient identity, acquired support, exact Add/Remove marks, saved labels, undo/redo, cancellation fences and reachable single-frame alignment. Do not read, move, publish or delete private MRI, private model weights, saved marks or original benchmark receipts. New acceptance inputs are synthetic. The reviewed tree already contained substantial earlier work; this remediation neither resets it nor claims it was all authored in this pass.

The starting commit is `5f0efa433b6c8b6672add9a30184ec1140b2b5a3` on `blader/siqi-chen/segmentation-sampled-plane-pruning`, then equal to `origin/main`. The CTO numbers below are independent of the earlier audit's F numbers.

### Remaining-audit continuation

The previous goal turn made progress: the CTO remediation completed TurboVac and merged. The merged tree is retained; its measurements are not rerun or relabeled merely because the branch changed. Remaining work starts from that merge on `blader/siqi-chen/audit-sparse-editing`, with the complete F1–F13 scope below still authoritative.

The first remaining implementation boundary was F3: separate source/window-dependent grayscale from marks, mask contours, crosshair and animation-frame-coalesced brush drafts; extend sparse changes through the label owner and durable writer. Its contract preserves original labels, exact hard marks, undo/redo, source ownership, failed-save recovery and sparse GPU updates, without a remount-losing debounce or unversioned shared mutable buffer. Acceptance compares long-gesture raster builds, whole-volume copies, written payload and pointer-to-paint work, then verifies the reopened result rather than merely a submitted save.

F3 ownership checkpoint: the initial copy-on-write design assumed that downstream heavy-operation preparation would always precede any retained label read. A direct callback regression disproved that assumption: requesting original detail, then editing again before the callback prepared its work, changed the callback's input. Keep the single editing owner, but borrow its current revision at the callback handoff itself. The alternative of copying every published mask preserves implicit snapshot semantics but retains the original per-stroke cost. Ordinary displays consume versioned views; history retains immutable patches; asynchronous readers receive borrowed revisions; storage captures its payload before yielding. A further legitimate reader that cannot obey this contract would reopen the representation decision, not justify an unrelated cache guard. The retained failing reproduction is `frontend/tmp/sparse-editing-borrow-handoff-before.log`; the corrected regression and acceptance below passed.

### F3 implementation and acceptance

**Implemented and verified.** [Detailed evidence](../../evidence/sparse-editing-2026-09-03/README.md) describes native-resolution raster caching, exclusive versioned mask views, borrowed asynchronous snapshots, incremental counts/GPU updates and schema-8 atomic 4 KiB chunk persistence. Hydration does not rewrite saved data; source/dataset/revision fences prevent stale saves. Legacy dense labels migrate on intentional editing, and retryable errors are distinct from a conflicting saved dataset.

The same normal-production workflow ran against the merged baseline and candidate fingerprint `e719d61c4eb8357201da974c126053978bd8c3dd6135036d3f61a25822ce43f9`. On a synthetic 256 × 256 × 128 native mask, five long Add/Remove gestures plus Undo/Redo reduced label writes from **56 MiB to 36 KiB**, draft ImageData allocations from **205 to zero** and whole-mask `.slice()` calls from **seven to zero**. All hard marks, saved intermediate states, reopened/restored data and final canvas pixels matched exactly. Mean pointer-event-to-canvas-command time was 7.828 ms versus 0.390 ms; this is not physical screen latency or a hardware-wide frame-rate claim.

TypeScript and lint passed; **373 focused tests**, the **3,183-test complete default suite** (54 existing skips, zero failures), and **all six existing normal-production browser workflows** passed. The full-suite and browser receipts record unchanged source fingerprints. The comparison, migration, rollback, missing-chunk, stale-writer and callback-lifetime evidence is linked in the report. No private fixtures were enabled, and no new aesthetic/anatomical sign-off is claimed.

The two baseline-build setup failures are preserved. Streaming the public archive and using its canonical filesystem root corrected them without changing the candidate. The app's compiled-input fingerprint is now distinct from the measurement-harness fingerprint, allowing an unchanged normal production bundle to be reused after a harness-only edit; probe builds still require full-source identity.

### F5 immediate capacity and cancellation boundary

**Historical PR #12 checkpoint; superseded by the scalable implementation below.** Export and restore shared the 512 MiB restored-payload ceiling. Export registered the exact eventual manifest descriptors before reading Blob payloads, assembling chunked masks, hashing or packaging. It rejected over-limit totals without a download. An AbortSignal stopped collection or paused packaging; closing/unmounting the dialog prevented late progress, downloads and close timers. The UI explained the limit and preserved existing data rather than presenting DICOM reimport as saved-work recovery.

[Detailed evidence](../../evidence/backup-capacity-2026-09-03/README.md) includes 141 focused tests and an ordinary production-browser export at source `be7fb717605cd21e811f819a56b21dae7a8739ac1e74d2b3bf8024a781c0c109`. Two composed native Blob cache inputs totalled 512 MiB; one imported synthetic DICOM added 1,054 bytes. Export rejected the aggregate with zero large-Blob reads, downloads or page errors. A 1 MiB sparse-model export then restored into a fresh context with exact model and DICOM bytes. Complete ZIP members now use STORE, and the generated container passes the same reader preflight; a small zero-filled model had reproduced a separate expansion-ratio failure under DEFLATE. Archives may be larger; input guards remain intact. Desktop/mobile screenshots were individually inspected at original detail: the first pass rejected misleading one-examination recovery advice; the corrected static warning passed. This does not revalidate earlier imaging aesthetics or establish anatomical quality.

The stricter preflight exposed a fixture boundary, not a reason to weaken validation: fake IndexedDB uses Node structured clone, which turns jsdom Blobs into plain objects. Scoped native-Blob fixtures preserve source and restored bytes, and the suite now checks the exact DICOM bytes. JSZip's cross-realm ArrayBuffer check was handled with a zero-copy local byte view. The smallest byte round trip passed before the focused suite; all failed attempts remain local.

All six existing normal-production workflows also passed in 45.1 seconds at that fingerprint. The first full-suite run found five old Blob/File-fixture failures in storage-integrity tests; those pass after using cloneable native values. After reduction, the [complete suite](../../evidence/backup-capacity-2026-09-03/unit-suite.json) passed **3,191 tests, zero failures and 54 existing skips**, with unchanged source fingerprint `3d347c9baea2f29cc1d4054239d6d073cc948e41682a516da2ba8b5cd8632c66` at code commit `a83816c6169b3f69e91bebf11b28c095c1a1293e`. The only post-browser code changes removed an error-constructor alias and combined imports; the browser receipts remain scoped to their recorded fingerprint. The focused reduction checks passed 262 tests, TypeScript and targeted ESLint. [PR #12](https://github.com/blader/MiraViewer/pull/12) carries this F3/immediate-F5 batch; its live state owns final CI/review/merge clearance.

At that checkpoint F5 was not complete: admitted operations still buffered payloads, the model cache remained outside the medical-data transaction, and large verified staging/atomic publication plus above-512-MiB corruption/quota/cancellation round trips remained required. The September 4 completion checkpoint below supplies that missing implementation and evidence without relabeling the earlier run.

### F5 scalable-backup continuation: contract and architecture

The preceding turn made concrete progress: PR #12 merged as `d4fb10fdfbf2d1e9aac44759eb2387ce1e5ba477`. Its measurements and private/failed receipts remain intact. The next work starts from that merge on `blader/siqi-chen/audit-streaming-backups`; no old bundle or acceptance run is restarted merely because the branch changed.

**Required result:** a backup larger than 512 MiB must export and restore exact scans, saved labels, hard marks, panel/acquisition settings, models and derived frames with bounded live buffers. Verified restoration is staged durably but remains invisible until one noninterruptible publication transaction. Corruption, quota failure and cancellation before that boundary must preserve every previously visible record, including model bytes. Legacy version-2 backups and legacy cached models remain readable. Browser evidence must use real generated bytes, record peak memory and exercise failure/cancellation, not only virtual sizes or a passing manifest calculation.

**Structural mismatch:** export retains whole payload buffers and JSZip output; restore retains every verified payload and writes a separate model database before the medical transaction. Raising a constant cannot fix either boundary. Cross-database rollback would add a second publication authority and can fail during recovery. A dataset-generation rewrite could make publication a pointer swap but would force unrelated medical readers/writers through a new addressing layer. Instead, retain existing medical stores and move the model API to a store in the same database, with one-time transactional legacy migration. Stage verified rows/chunks under operation-owned keys and publish from that staging store in the existing medical transaction.

**Export/read path:** retain the versioned manifest and ZIP safety policy; remove eager JSZip payload accumulation. Read and hash bounded chunks, preserve SHA-256 and CRC integrity, and provide a streaming destination while keeping the ordinary download path usable. The later browser proof must distinguish file-backed Blob references from actual JavaScript/RSS growth; a renamed buffered exporter is not completion. The [incremental SHA-256 API](https://github.com/paulmillr/noble-hashes#implementations) is a candidate for retaining digest verification without whole-payload WebCrypto buffers; adopting it requires ordinary dependency and known-vector checks.

**Execution and falsifiers:** first prove model/medical atomicity with a late-write failure and legacy-cache migration; then stage bounded payloads and remove aggregate materialization; then validate true above-cap export/restore, failure preservation and memory in the built app. If staged publication itself retains payload-sized buffers or cache migration creates a second visible model authority, reopen this design before adding guards. One heavyweight validation runs at a time. Existing unit/browser evidence is retained at its own fingerprint; final whole-suite and browser verification wait for the implementation boundary to settle. No F5 completion is claimed by this plan.

## Findings and changes

| Review finding                                   | Implemented change                                                                                                                                                                                                                                          | Verification boundary                                                                                                                     |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| CTO1: settings-read failure locks viewing        | Failed hydration creates an ephemeral browsing owner, without verified write authority. Slice navigation, pan and zoom remain usable; Retry reloads saved settings without writing defaults.                                                                | Hook read-failure/token tests and normal-app reload, browse, Retry, restore and clear workflow.                                           |
| CTO2: no exact acquired plane                    | Default **Exact source pixels** uses nearest acquired samples, DICOM window/inversion and an opaque section, without translucent tissue tint. **Blend with anatomy** remains explicit. Opaque nearer anatomy may occlude; contours are separate overlays.   | Production-shader independent grayscale oracle from both directions; normal 3D controls; no intensity re-upload for display-only changes. |
| CTO3: custom inference inherits assembler budget | Native assembly keeps 512 MiB. Custom inference uses the device-derived learned envelope, with conservative model/runtime/tensor/source/cache allowances.                                                                                                   | 128³/20 MiB admission regression, oversized-input rejection, real 20 MiB synthetic model in the browser, sampled renderer memory receipt. |
| CTO4: pending content unmounts overlays          | Keep annotation children mounted but inert while pixels/alignment are pending. Preserve viewport focus and pan-handler identity while rejecting pending gestures.                                                                                           | Component identity/focus tests and ordinary image retry workflow.                                                                         |
| CTO5: reads write and each pan rescans ownership | Catalog and settings snapshots use readonly transactions. Explicit open/import/restore initialization owns default acquisition writes. Hydration returns frozen verified sources; saves check the dataset token and put one canonical row using two stores. | Transaction-level DB tests, additive import batches, source conflicts, stale writers, legacy projection and real acquisition switching.   |
| CTO6: competing memory registries and estimates  | `operations.prepare(kind)` in the existing imaging context is the single preparation boundary. Viewer/editor/selection-runtime owners quiesce and report retained bytes there. The driver receives the already admitted literal-mark runtime allowance.     | Heavy-entry handoff tests, exact admitted/retained allowance checks, real retained-session correction and custom-model replacement.       |
| CTO7: presented order invalidates physical work  | Remove presented ordering from request identity; read the current presented set before the next target. Keep in-flight physical work and prioritize newly visible remaining work.                                                                           | No-abort hook regression and five-examination cold/warm visible-pair browser workflow.                                                    |
| CTO8: native window ignores DICOM VOI            | Prefer valid DICOM display windows, including threshold windows; fall back to finite measured bounds. Slider bounds remain the full source range.                                                                                                           | Window/inversion/threshold tests and exact shader oracle.                                                                                 |
| CTO9: guide and ledger contradict implementation | Replace stale main-realm and no-reuse claims; distinguish the fresh final-ranking worker from the retained segmentation runtime; update F13 status. Move narratives to historical storage.                                                                  | Guide read against live owners; one current ledger; original evidence preserved.                                                          |
| CTO10: alleged token-initialization race         | **No change:** the review refuted this diagnosis. Preserve serialized transactional initialization and the existing write fence.                                                                                                                            | Existing dataset-token and replacement tests.                                                                                             |
| CTO11: resident validation throws during render  | Catch resident-plane construction errors at the viewer boundary and render a local notice.                                                                                                                                                                  | Malformed support regression; no error-boundary failure.                                                                                  |
| CTO12: no-source dates create legacy writes      | Only selected, verified study/series sources can persist. A display date alone grants no write authority.                                                                                                                                                   | Hook no-source tests and canonical-row DB tests.                                                                                          |
| CTO13: patient backup leaks foreign app state    | Whitelist owned acquisition keys and owned patient/study selection. Exclude the live writer token; ignore foreign acquisition preferences during old-backup restore.                                                                                        | Export/restore round trip with foreign and injected legacy app-state rows.                                                                |
| CTO14: filmstrip owns no keyboard shortcuts      | Accept Space, arrows and 1–9 from the filmstrip's own date buttons; keep other controls/dialogs protected. Suppress native repeated Space activation.                                                                                                       | Keyboard/focus tests.                                                                                                                     |
| CTO15: localizer owns global bounds              | Choose a full stack for the shared reference when available; use a localizer only if no full stack exists. Alignment and navigation share the same selection helper.                                                                                        | Localizer-first reference and navigation tests.                                                                                           |
| CTO16: retry duplicates pending decode           | Retain/reuse a pending image promise. Evict only a settled rejected decode when retrying.                                                                                                                                                                   | Deadline/retry test asserts no second decode while the first is pending.                                                                  |
| CTO17: native decode failure blanks safe content | Keep the last accepted same-volume/source plane on a frame failure and show the error. Clear it on ownership revocation; never show a foreign source.                                                                                                       | Same-source failure and revoked-identity regressions.                                                                                     |
| CTO18: final ranking orphans Elastix             | Dispose/invalidate the shared Elastix worker before replacing its reference and on every operation exit.                                                                                                                                                    | Existing cancellation/failure registration tests plus disposer assertions.                                                                |
| CTO19: unused image-producing runner             | Restrict historical image production to unit/browser-test mode. Application callers use pose-only estimates. Retain the synthetic image/pose parity oracle.                                                                                                 | Worker mode guards and actual browser image-versus-estimate parity.                                                                       |
| CTO20: failed tracking job retains started state | Per-job `finally` closes the channel and resets waiting/started state. Runtime/provider reset survives disposal failure.                                                                                                                                    | Failure followed by a new job on the same worker.                                                                                         |
| CTO21: exact-plane rays cannot terminate early   | Exact rays keep the full-volume step pitch, stop at the plane and terminate on opaque foreground. Blend mode retains its far-side anatomy contract.                                                                                                         | Pixel oracles and separately reported plane-off/blend/exact settled-frame measurements.                                                   |
| CTO22: 44-degree source labeled cardinal         | Require a source normal within 30 degrees of a cardinal axis. Preserve axis reversal and genuinely near-cardinal sources.                                                                                                                                   | 30/31/44/45/46/59-degree classification cases.                                                                                            |
| CTO23: custom advisory measured on render        | Build the custom-model preflight on action, after shared preparation. A rejected old estimate remains advisory; Retry remeasures current owners.                                                                                                            | No-render-measurement test; retry after memory changes.                                                                                   |
| CTO24: independent-2D Auto-fill capability       | Document the intentional change in the guide, README and reviewer material: independent-2D stays brush-editable, but Auto-fill requires a compatible accepted native source.                                                                                | Capability/editor tests; explicitly not presented as a behavior-neutral refactor.                                                         |

### Important details

**Source-owned settings.** Safe legacy rows are projected in memory during readonly hydration, not migrated by reads. The first intentional canonical save preserves legacy provenance; ambiguous rows still require explicit assignment. Unrelated imports do not replace the current patient's manual adjustments or browsing progress. Restore/recreation rotates the saved-work token, retiring old callbacks and rejecting queued/cross-tab writers atomically.

**Heavy-operation ownership.** Reconstruction, refinement, enhancement, selection, custom inference and model-file replacement all prepare through the imaging context. The caller's retained editing snapshot is counted as a floor, not added again to the current editor. The admitted runtime estimate is passed unchanged to the tracking owner; subsequent compressed prompts cannot lower it. Custom workers terminate on result, error, abort or deadline and are never retained. EfficientTAM sessions may be retained for 30 seconds, but each job gets fresh history and channel state.

**Exact versus blended display.** Exact mode is calibrated at the source plane, not a claim that the whole volume render is diagnostically calibrated. Unsupported samples remain absent, nearer opaque anatomy may occlude, and a selected contour can overlay the plane. Blend mode deliberately changes luminance for anatomical context. Invalid source identity clears content; an ordinary same-source decode failure preserves the last good plane with a notice.

**Memory evidence.** The new 128³ synthetic oracle contains 20 MiB of real weights used by a data-dependent Gather, not filler metadata or an unused initializer. It proves this model's execution and output, not arbitrary model complexity. Chromium memory dumps sample the worker-host renderer, including its page. They are neither isolated worker allocation nor a guaranteed instantaneous/OS-release peak. The admission policy is not an allocator cap.

## Repository and delivery hygiene

- `.baton/` and new raw `artifacts/` outputs are ignored, not deleted. Previously tracked reviewed images remain preserved. Vercel CLI uploads also exclude handoffs, artifacts and agent documents.
- Pinned public ONNX graphs are LFS-managed through `.gitattributes`. Hash/size verification fails helpfully on an unresolved pointer. Local LFS hooks were installed. MiraViewer's Vercel Git LFS setting was enabled and independently read back on September 3. The first TurboVac publication uploaded all four ONNX objects (75 MB); Git-deployment hydration is checked separately from successful upload.
- Generated DICOM files and large synthetic model bytes stay out of Git. Checked-in generators reconstruct fixtures; the 20 MiB model helper stores a compact generation recipe.
- `tsconfig.browser-tests.json` is a TypeScript project reference. Browser probes/specs now receive typechecking, not just ESLint parsing or runtime checking.
- Seventeen older execution plans moved to `exec-plans/historical/`, with their links rebased and historical status explicit. The CTO input stays at the user-specified path. Original pre-redaction documents and hashes are retained under ignored `artifacts/private-authoring/cto-review-2026-09-03/`.
- Reviewer evidence belongs under `agent_docs/evidence/`; copies must be path-scrubbed and identify their original receipt hash. Original receipts, source captures, fixtures, MRI, marks and models remain protected.

## CTO acceptance record, before publication reduction

Source fingerprint for this acceptance batch: `dfb91f63584e58614c723e3e660d7b4c88c46d03857e67a02b6fde97b65217c4`. [Curated receipts and limitations](../../evidence/cto-review-2026-09-03/README.md) preserve original receipt paths and hashes. These remain scoped measurements of that source, not new post-reduction runs or remote CI clearance.

| Check                                    | Final result                                                                                                                                                                                                                                                                          |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| App/config/browser TypeScript references | Passed (`npx tsc -b`).                                                                                                                                                                                                                                                                |
| ESLint                                   | Passed.                                                                                                                                                                                                                                                                               |
| Complete Vitest suite                    | **3,169 passed, zero failed, 54 skipped**, with two workers. The three earlier import-initialization assumptions were corrected; focused import and disposal checks also passed.                                                                                                      |
| Production and browser-test builds       | Both passed; copied pinned model assets verified. Existing large-chunk warnings remain part of the broader startup audit.                                                                                                                                                             |
| Normal production browser workflows      | **6 passed**: source-owned settings/acquisitions, decoder retry, browse after failed settings read, visible Retry on desktop/mobile, custom model save/cancel/reopen, restore/clear and cold/warm alignment. One pan produced one two-store write and zero study/series scans.        |
| GPU                                      | **37 pixel checks passed** on headed Chrome / ANGLE Metal / Apple M4 Max. Completed-frame medians: plane off 13.98 ms, blended 13.19 ms, exact 10.30 ms. Three samples/mode with synchronized readback; not a general UI-speed claim.                                                 |
| Performance browser boundaries           | **3 passed**: coarse image/pose parity, final-scoring worker responsiveness/cancel, and large derived-frame storage.                                                                                                                                                                  |
| Actual custom/pinned inference           | **3 passed**: real 128³/20 MiB custom graph, slow-graph cancellation/recovery, retained corrections and verified offline model bytes.                                                                                                                                                 |
| Browser lifecycle                        | **13 tests passed**, unchanged source fingerprints, each owned batch `Cleanup: CLOSED`. No stale owned resources remained in the final sweep.                                                                                                                                         |
| Visual inspection                        | Actual-app capture and DOM/input checks completed. Raster/aesthetic approval withheld under `visual-validation`: apparent model-side preview corruption was absent from canonical pixel checks. Earlier visual ratings on that path are quarantined; no architecture depends on them. |

### Custom-model calibration and retained limits

The final 128³ run used 20,972,050 encoded bytes, a 427,821,128-byte working allowance and a 256 MiB runtime floor. It returned 2,097,152 labels with zero mismatches in 480.47 ms through worker completion under tracing; the source was unchanged. Sampled renderer private footprint reached 372,248,576 bytes. The earlier 128 MiB reserve undershot observed renderer growth in a repeat run and was increased without restoring the incorrect 512 MiB assembler ceiling.

These measurements include a worker-host page, use asynchronous process dumps and do not establish an isolated worker peak, instantaneous maximum, immediate OS reclamation or arbitrary-model bound. The device envelope and full retained-owner accounting remain separate from the measured example. All results, including the rejected initial GPU timing, the original three unit failures and the recovery screenshot limitation, are preserved rather than overwritten as successes.

Raw final evidence is in `artifacts/performance/audit-cto-2026-09-03/attempt-03/`; earlier attempts remain protected. Six completed generated build bundles are released with no remaining consumer from this audit, recorded in `released-builds.json`. Canonical source, model weights, MRI, marks, saved captures and receipts were not deleted.

## Broader audit scope remains separate

The original F1–F13 contract and later N/A-numbered findings are preserved in their linked audits and historical checkpoints. This CTO remediation does not silently cancel or close them.

| Original audit area              | Current status, without overstating closure                                                                                                                                                                                                                                                                                                                      |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 alignment scoring             | Coverage-only reverse scoring, pose-only coarse work and fresh-worker final ranking are implemented; actual-worker parity/responsiveness receipts exist. Current whole-suite and worker/browser acceptance passed for this remediation; broader anatomical scope remains separate.                                                                               |
| F2 source/model lifetime         | Accepted metadata/intensity context and admitted 30-second runtime retention are implemented. Small synthetic retained-session parity exists; large anatomical/hardware cases remain separate.                                                                                                                                                                   |
| F3 sparse editing/persistence    | **Implemented and verified.** Native raster caching, versioned copy-on-write editing and atomic chunk updates preserve exact history, marks, reopen/backup and GPU behavior. Synthetic baseline/candidate evidence above.                                                                                                                                        |
| F4 derived-frame storage         | Key-only eviction and indexed hydration are implemented; prior 160 MiB browser evidence is retained.                                                                                                                                                                                                                                                             |
| F5 streaming/staged backup       | **Implemented and verified.** Incremental export, durable verified staging and one model/medical publication transaction replace the temporary aggregate cap. Physical 2.02 GiB restoration and cancellation/corruption/late-quota preservation passed; per-file and resource limits remain.                                                                     |
| F6 durable source identity       | Source-owned settings/acquisitions, stable source-grid selection keys and unique exact legacy recovery are implemented and exercised. Selective backup retains settings ambiguity. Unknown or competing historical grids remain preserved without automatic assignment. See the [F6 report](../../evidence/durable-identity-2026-09-04/README.md).               |
| F7 warm native rendering         | **Implemented and locally verified.** Exact replay plus one bounded idle reslicing runtime; automatic playback coalesces behind current visible results. Real 256²/512² comparison closes cancellation starvation, with 97 exact pixel/support pairs and sampled memory tradeoffs. See the [F7/F9 report](../../evidence/startup-delivery-2026-09-04/README.md). |
| F8 unreachable paths             | Production legacy segmentation/capture paths are removed; required geometry and historical test oracles remain.                                                                                                                                                                                                                                                  |
| F9 startup/host parity           | **Implemented; deployed candidate qualification pending.** Unused tools removed, dialogs deferred with focus/recovery, Vercel isolation configured. Real preview/dev/offline runtime, codec and asset checks pass. Public baseline is confirmed unisolated/single-threaded; it is not candidate acceptance.                                                      |
| F10 usable capability/failure UI | Main capability/retry/keyboard paths are implemented. This review adds browse-on-settings-failure and visible recovery.                                                                                                                                                                                                                                          |
| F11 reproducible acceptance      | **Implemented and verified.** Repository-owned lint, production type/build, full units and all ten synthetic production-browser workflows passed in run 33878900501. PR #15 merged with the exact tested tree; private anatomy and hardware-performance claims remain separate.                                                                                  |
| F12 legacy geometry enrichment   | Implemented and verified: shared versioned manifest enrichment, bounded headers, interrupted/retried batches, duplicate repair, schema-6 physical viewing and exact saved-work backup restoration. See the [source-metadata report](../../evidence/source-metadata-2026-09-04/README.md).                                                                        |
| F13 custom worker lifecycle      | **Implemented**, including manifest/source/output guards, bounded worker lifecycle, fresh-provider recovery and device-derived admission. Real cancel/recovery/custom-oracle evidence exists; no blanket arbitrary-model memory claim.                                                                                                                           |

## TurboVac publication checkpoint

The requested review fixes are kept together in [PR #11](https://github.com/blader/MiraViewer/pull/11); no PR-splitting work was performed. First clean-pr and diff reduction completed before this publication checkpoint. The live PR is the authority for subsequent current-head CI, review and merge state; this implementation ledger does not duplicate that changing state as a static checklist.

The user has authorized TurboVac and merge of this branch. The publication pass preserves the synthetic receipts above, excludes raw artifacts and private inputs, and keeps the broader audit's open scope explicit. Six TypeScript files received formatting-only changes after the recorded fingerprint; those receipts remain evidence for the exact source they identify, not a claim of remote CI clearance.

First publication checks after formatting: TypeScript passed; lint passed with two hook-dependency warnings subsequently fixed during reduction; the six focused test files passed 143 tests with 23 existing skips. All four public ONNX files were verified as Git LFS pointers in the index. Public candidate scanning found no credential patterns or private local paths (the review contains only an explicit `/Users/<local-user>` placeholder); DICOM UIDs in the curated receipts match the synthetic fixture generator. The published evidence README and both PNGs returned HTTP 200, correct content types and exact local hashes.

### Reduction result

The actual base is `origin/main`, merge base `5f0efa433b6c8b6672add9a30184ec1140b2b5a3`. Two full-file analysis passes covered 403 base/current file versions and all 1,612 diff hunks. The first read 193,903 text lines; the final pass read 193,889. Code/JSON parsing, line coverage and complete diff receipts are preserved under ignored `artifacts/turbovac/2026-09-03/reduction/`. Whole-file automated coverage was combined with direct architectural review of candidate implementations and their callers; this is not a claim of manual inspection of every preserved prose/lockfile line or anatomical/aesthetic approval.

One normal reduction commit, `d8686ab73f700548f7449da9d5d02cdac89728ca`, made three source-file changes: common settings-owner publication after a read, direct use of the existing optional-source API, and precise callback captures for editor/viewer registration. Ordering, null write authority after read failure, cancellation, retained-byte accounting and all comments remain unchanged. Formatting, lint, TypeScript and **285 focused tests** passed. The final pass found no further viable scope-preserving cut; the temporary `trim-codex.md` was removed, and the worktree was clean before second clean-pr.

| Reduction-window metric | Before |  After |
| ----------------------- | -----: | -----: |
| Full PR insertions      | 27,851 | 27,837 |
| Full PR deletions       |  8,395 |  8,395 |
| Full PR churn           | 36,246 | 36,232 |
| Non-comment insertions  | 27,556 | 27,542 |
| Non-comment deletions   |  8,012 |  8,012 |
| Non-comment churn       | 35,568 | 35,554 |
| Comment-only additions  |    295 |    295 |
| Untracked code lines    |      0 |      0 |

This is a 14-line reduction: 0.0508% of non-comment insertions and 0.0394% of non-comment churn; runtime TS/JS non-comment insertions fell from 4,015 to 4,001 (0.3487%). Prose, receipts, locks and generated assets remain included in the full-PR totals. Comment accounting includes TS/JS comments, Python comments/docstrings, CSS/HTML blocks and Git configuration comments; mixed code/comment lines and embedded-language strings count as code. These are the reduction-window measurements, before later validation and publication bookkeeping.

The larger proposed cuts were rejected for specific reasons: per-run scoring, one-shot custom inference and retained tracking require different worker lifetimes; main/worker snapshot checks protect separate copy boundaries; existing database-reset helpers predate this diff; identical gray-interior assertions bind different historical/candidate solvers; and legacy settings, model derivation, numerical oracles and saved evidence still have required consumers. Common acquisition identity/counting already has shared helpers. No test or compatibility boundary was removed for line savings.

### Post-reduction verification

The first full run at source `a0334f1028a6593d2fb587658838bb8c5792ebe078f2462ac3681a7461ec17c0` passed 3,168 tests, with one full-size native-context test hitting Vitest's default five-second watchdog. Its isolated 43-test file then passed, with the affected case taking 1.19 seconds (the earlier CTO full run took 0.98 seconds). The test checks memory/source admission on the real 221-frame fixture, not a latency target; only that test's watchdog was made explicit at 15 seconds, with its fixture/assertions unchanged. The follow-up focused file passed all 43 tests. The failed full run and both focused receipts are preserved.

The second full run passed **3,169 tests, zero failures, 54 existing skips** across 167 passing and six skipped files. See the [current unit receipt](../../evidence/cto-review-2026-09-03/turbovac-unit-suite.json). Current validated source fingerprint: `526789ca62d20cd3870ea065558d589c8f16ec199897e54d6da1e582bcaeb4fa`, code/test commit `34a67c42d5be6c6e6ddbc2de6387ab8af77f48b8`; later publication changes only add documentation/evidence. TypeScript, final full lint (zero warnings), focused formatting and the isolated production build passed. The copied model allowlist passed its size/hash verification; existing large-chunk warnings remain part of the broader startup work.

All **six normal production browser workflows** passed at that fingerprint in 41.6 seconds, using synthetic scans and disposable headed Chrome at `http://127.0.0.1:43134/`. The [current run receipt](../../evidence/cto-review-2026-09-03/turbovac-workflow-run.json) records unchanged source, all owned resources closed, no remaining profiles and a released port. The measured pan still produced one `app_state`/`panel_settings` write transaction and zero source-catalog reads. Source adjustments survived acquisition switches/date collisions; custom-model draft bytes survived save/reopen; cancel/reconstruction replacement retired workers; read failure/Retry, restore, annotations and cold/warm visible alignment passed.

The desktop/mobile recovery PNGs were byte-identical to the preserved captures, so no duplicate images were published. **Raster/aesthetic sign-off remains withheld** for the earlier image-preview failure; DOM/input/persistence checks and file identity do not substitute for reliable aesthetic inspection. Storybook was not used. Original GPU, large-model calibration and inference receipts retain their original fingerprint and limits; they were not rerun or relabeled as current measurements.

The new generated production bundle was released after its consumers closed, not deleted. Its exact location is in ignored `artifacts/turbovac/2026-09-03/released-builds.json`. All canonical weights, MRI, marks, authored harnesses, raw attempts, captures and receipts remain protected.

Merge requires current-head CI/review/mergeability clearance after the refreshed publication. No stage is complete merely because tests, a document or a local patch exist. The larger audit goal remains unfinished.

### Scalable-backup implementation checkpoint: one publication database

Schema 9 adds the model store to the MRI database. Model APIs first copy legacy cache records transactionally, preserving bytes and timestamps; the legacy version upgrade retires old writers, and originals remain intact after migration. One durable migration marker prevents deleted models from reappearing on reload. Cache reads and ordinary model-pair changes now use the shared database; the separate connection owner was removed. Restore puts models in the same transaction as scans, labels, settings and the saved-work token, explicitly aborting all writes on any failure. Imported app state cannot alter internal migration controls.

A regression first proved the old failure: rejecting a conflicting patient name nevertheless changed the saved model to the archive's bytes (`frontend/tmp/streaming-backup-model-atomicity-before.log`). It now preserves the previous model. Three focused files passed 158 tests, including real legacy-cache migration/old-writer retirement, exact model bytes, late model-write quota rollback of scans and chunked labels/marks, post-request model-pair cancellation, restore's noninterruptible commit, and reload without resurrecting obsolete cache data. App/config/browser TypeScript references and focused lint passed. One authored test attempt failed on a missing constant import; its failed log remains preserved, and the corrected run passed.

This was a necessary storage boundary, not F5 completion by itself. Payload staging, streaming hashes/output, above-cap built-app round trips and measured memory/cancellation were still unfinished. No new browser/model run or production deployment was claimed for this checkpoint.

### F5 resource-proof checkpoint: September 4

The staged writer, bounded hashing/ZIP output and direct native-file destination were implemented. TypeScript, focused lint, 214 scoped tests and seven database tests passed. Schema 10 preserves schema-9 models while adding operation-private restore staging. The normal build at this checkpoint had application fingerprint `04b60314aa9a36b39b4403cc35023a39df0ea43ca42774306c3b91d84bc33a25`; the large restore was still pending.

The first large attempt produced a real 2,173,276,563-byte archive; Python independently read every referenced member and checked CRC/SHA. Export cancellation settled without a download, and full export used at most 65,557-byte Blob array-buffer reads. The browser test then used the empty-state import selector on a populated page and stopped before restore. Its archives, original trace, recovered phase results and memory samples remain under ignored `artifacts/performance/audit-streaming-backups-2026-09-04/attempt-02/`.

**Architecture/measurement reset:** observed aggregate RSS was already several GiB before export and peaked at 5,817.7 MiB after source replacement. The harness used Playwright non-persistent contexts, whose browsing data is not written to disk; Chromium's incognito IndexedDB is in-memory. That is not the ordinary disk-backed persistence boundary. Compare a changed exporter or an OPFS-only redesign against correcting the producer: keep application code unchanged, use separately owned temporary persistent profiles for both source and destination, retain the existing build, and add per-process RSS samples and durable stage checkpoints. The next proof must finish cancellation, corruption, late quota failure and real multi-GiB restoration. If persistent-profile buffers still grow with the whole payload, reopen the exporter instead of claiming bounded memory. See [Playwright's context contract](https://playwright.dev/docs/api/class-browsercontext) and [Chromium's in-memory bucket predicate](https://chromium.googlesource.com/chromium/src/+/c0811f657d5ae8c42a9fb827e7e36e7e58a16bab/content/browser/indexed_db/instance/bucket_context.h).

### F5 scalable-backup completion: September 4

**Implemented and verified.** [The detailed report](../../evidence/streaming-backups-2026-09-04/README.md) records a physical **2,173,276,563-byte (2.02 GiB)** ordinary export and restoration into a separate disk-backed browser profile. Independent Python CRC/SHA verification covered all 147 payload members. Exact recovery included 128 DICOM files, an 8 MiB label volume, literal Add/Remove marks, source-owned settings, derived pixels/support and 16 synthetic 128 MiB model payloads. A separate 40.60 MiB native-file export also restored exactly. No private MRI or private weights were used.

Schema 10 and the existing model API provide one publication authority. Export incrementally hashes/writes 64 KiB chunks and retains immutable Blob references for ordinary downloads. Restore durably stages verified payloads and sparse label chunks, then publishes medical data, models and the saved-work token in one transaction. The temporary aggregate cap is removed; per-file 512 MiB, archive metadata, ownership and integrity guards remain. Space admission accounts for staging plus publication. Cancellation before publication, a physically corrupted member and a late model-write quota failure all preserved the complete prior visible dataset and token. Export cancellation settled without a download; restore cancellation reached the canceled UI in 54 ms.

The completed disk-backed run reused the unchanged application build from the preceding checkpoint. Sampled aggregate browser RSS peaked at **2,373.4 MiB (2.32 GiB)** across two browser instances during the large restore; this is a sum of process RSS sampled every 500 ms, not unique memory or a universal bound. Large export took 77.55 s including saving its download; fresh restoration took 71.07 s. Legacy compressed entries and one export selection remain per-entry memory costs. Native-file testing used a real OPFS writable stream with only the OS picker substituted. Neither storage fixtures nor static modal captures establish anatomical/model quality or hardware motion.

A subsequent safety pass separated lock-owned staging keys from producers without Web Locks, so abandoned-stage cleanup cannot remove another uncoordinated live attempt. The namespace regression passed. The complete default suite then passed **3,209 tests, zero failures and 54 existing skips**, with unchanged source fingerprint `18da86c9f15d6242835c4a973c336253b3a2fe6fa7dbb6c8ee8a3d0f8ef70435` and application fingerprint `7c7631bda4c3bb809dd8201e3ce3249a666b598fca792604143e186b561aa619`. TypeScript, full lint, the normal production build and **all seven normal-app browser workflows** passed at that tree; the large resource receipt remains scoped to its earlier fingerprint.

Desktop/mobile export captures were individually inspected at original detail and passed within their static layout scope. The large-restore summary capture was separately inspected, with below-fold consent exercised by the test. Storybook was not used. Current and stale task-owned browser resources were verified closed; user/other-worktree state was preserved. Raw failed attempts, large generated ZIPs, authored harnesses and receipts remain local and protected; only curated synthetic evidence is published. F6/F7/F9/F11/F12 and the larger audit's other stated limits are not closed by this F5 result.

### F5 publication reduction and final checks: September 4

[PR #13](https://github.com/blader/MiraViewer/pull/13) contains the scalable backup work. Its reduction commit `4824b5ad08da75be5f2a6a512ff943a9eb572537` removes dead adapters and repeated cancellation/verified-read plumbing while keeping native-file abort ownership explicit. Two complete passes found no further safe scope-preserving cuts; non-comment code additions fell 6.6% and non-comment code churn 8.8%, with no comment text or original evidence removed. The existing write-failure regression now also covers synchronous abort failure.

The [post-reduction receipt](../../evidence/streaming-backups-2026-09-04/post-reduction.json) records **3,209 passed tests, zero failures, 54 existing skips**, TypeScript, full lint, a production build and **seven normal-app workflows in 43.3 s**. Source/application fingerprints `2933545c9088654bc1f85afcd2a159d6e2eccc8a0c899afd65c8bda68538c570` / `b78d091699bd7b5dc8b302542983ad70db7e062e9f839c8c450a620d45606e6a` stayed unchanged. Desktop/mobile capture bytes match the individually inspected originals. A read-only audit of 20 current/prior task receipts found no remaining owned browser, server or profile. The earlier multi-GiB measurement retains its original fingerprint; this checkpoint does not claim a new large resource run or remote CI/review clearance.

### Source-metadata continuation: September 4

The preceding goal turn made progress: PR #13 completed TurboVac and merged as `76e64347f045b07a041a40af8e01d00373d838ce`. That implementation and its exact receipts remain retained. Work continues from the merge on `blader/siqi-chen/audit-source-metadata`; the full F1–F13 objective remains open.

The next boundary is F12, followed by the remaining F6 durable-volume identity work. Current source-owned panel settings already survive acquisition switches and date collisions, but a legacy image with valid position/orientation/spacing is still rejected when its derived physical-position field is absent. Duplicate import skips enrichment, while a separate legacy acquisition-header hydrator runs only in the 3D classifier.

**Contract and approach:** preserve admitted SOP/series/study identity, original Blob bytes, settings, marks, labels and dataset publication tokens. Move bounded legacy geometry/acquisition enrichment to one versioned manifest boundary, reuse its header extraction in ingestion, and let duplicate imports fill genuinely missing metadata without replacing the image. Derive ordering from validated geometry; absent or malformed geometry remains unavailable. Completed per-image metadata is the resume authority, not another job ledger. Avoid a database-open full-payload migration and a second independent geometry hydrator.

**Proof and checkpoint:** first reproduce missing-position rejection; then verify bounded header reads, interrupted/retried batches, stale-dataset rejection and duplicate enrichment. Exercise an older-schema synthetic database through physical admission, reopen and backup/restore, preserving exact saved work. Current canonical manifests must retain their single-snapshot fast path. If the design requires changing source pixels, dropping marks, guessing ownership, or repeatedly parsing completed headers, reopen the ownership decision before adding another workaround. Run the final whole suite/browser checks once this boundary settles, not after each intermediate edit. No new completion claim is made by this plan.

**Execution-boundary checkpoint:** source inspection showed that moving hydration into individual manifest reads would multiply its four-reader limit under the 3D workspace's parallel acquisition loading. This is a structural concurrency mismatch, not a measured out-of-memory event. A shared cross-component queue could limit it but adds cancellation and scheduling ownership. Instead, the existing workspace loads acquisitions sequentially, retaining four bounded header reads within that operation; alignment passes its existing controller and source scope into the same boundary. The held-first-manifest/unmount regression is the direct proof. If independent acquisition requests still multiply this operation's header work, reopen the design rather than introducing another memory estimate.

Validated stored geometry is reused without parsing image bytes. Missing or invalid cached geometry may be repaired only from that image's UID-verified original header; conflicting valid identity/geometry is rejected. Numeric components must be complete decimal/exponent values, not JavaScript prefixes, hex or silently discarded missing components, consistent with the [DICOM Decimal String definition](https://dicom.nema.org/medical/dicom/current/output/chtml/part05/sect_6.2.html). Existing comma/space-separated vector compatibility remains. This does not claim full DICOM conformance or anatomical acceptance.

**F12 full-suite checkpoint:** the first full run passed 3,210 tests, with 54 existing skips and nine failures. Eight failures were diagnostic compatibility: earlier geometry/patient guards hid the existing actionable error wording. The ninth fixture represented "unreliable geometry" solely by omitting the cache field that F12 is intended to recover. Preserve the dimension/patient diagnostics at the shared validation owner, remove the now-redundant native dimension guard, and make the invalid-source fixture omit actual patient-position metadata. The broader route remains unchanged; no retry, bypass, or new migration layer is warranted. A source review also identified the first-frame-missing case for conflicting frame-of-reference IDs; validation now uses the first known source ID and tests both cases. Focus these checks before the next full-suite replay and built-app proof.

The copied browser runner listed the prior sparse-editing harness in its provenance while correctly executing the source-metadata config. Preserve both earlier attempts unchanged and archive the recovered pre-correction runner; subsequent receipts hash/copy the actual source-metadata harness. This corrects evidence bookkeeping, not application behavior.

### F12 source-metadata completion: September 4

**Implemented and verified.** The [detailed report](../../evidence/source-metadata-2026-09-04/README.md) records one shared extraction/merge service and a versioned manifest preparation boundary. Existing validated geometry supplies missing ordering directly; otherwise four bounded original-header reads feed 32-row atomic batches. A completed per-image version owns resumption, not a separate migration ledger. Cancellation, source conflicts and changed dataset/patient scope fail safely. Modern manifests retain a single readonly snapshot, and unchanged duplicate imports retain their fast path. Metadata-only reimport preserves the source Blob and saved-work identity and reports the repair explicitly.

The synthetic older-schema browser case upgraded schema 6 to 10, admitted all 24 source frames, opened the actual selection view, and retained physical metadata through reload and fresh-context ZIP restoration. All independent generated-image hashes matched the original, upgraded and restored browser records. Source-owned settings, exact label bytes, reviewed state and literal Add/Remove marks survived. The 35-frame unit cancellation case retained 32 committed rows and read only three on retry. Unknown or malformed source geometry remains unavailable; legacy label-grid preservation does not imply automatic remapping onto a different grid.

Final verification passed **3,221 tests, zero failures, 54 existing optional skips**, TypeScript and full lint. **All nine normal-production browser workflows passed in 43.3 seconds.** Source/application fingerprints were `a98b2533a14d8a8301cbd3386c9cd8ae7de5235af5efbf28c2575ab14d2d44af` / `5546d0ced083fb92f6c7dcf134c4c1a61b2b521a121958070f5690c1c9d1b27b`. The production bundle was reused after correcting only the browser test's missing Select tissue action and adding captures; its original manifest was retained. Parent HEADs in uncommitted-candidate receipts are not the implementation commit.

Desktop/mobile repair summaries and the opened/restored axial editing states were individually inspected at original detail. The final UI captures matched the inspected bytes. These are scoped static verdicts, not anatomical, full-page layout or motion sign-off. A scoped audit of 24 current/prior receipts found no owned browser, server or profile; user/other-worktree state was preserved. Failed attempts, original ZIPs, authored harnesses, captures and receipts remain local and protected. First clean-PR publication is now in progress; reduction, second clean-PR, Hall Monitor and merge still require their own observed completion. F6/F7/F9/F11 remain separate work under the original goal.

### F12 publication reduction and final acceptance: September 4

[PR #14](https://github.com/blader/MiraViewer/pull/14) contains this metadata-recovery work. The first clean-pr completed at `f064e649bf5bcae6f4712d1630dcf3d88e94654a`. A complete reduction review then exposed a real untested consumer: opening Focus region immediately after lazy preparation displayed a blank image because two independent preview reads retained old geometry, despite the repaired stack enabling Open 3D. The original nine-workflow pass did not open that control and does not prove it worked. Attempt 05 preserves the failing actual-app reproduction.

The normal reduction commit `164cb0989f560009c2b2c37ab3d908c9733e7736` makes prepared manifests the preview authority, removing both loaders and four metadata/error state fields. It retains same-source focus intent, retires replaced identities and permits valid individual-source inspection under group failure without enabling unsafe reconstruction. Centralized unchanged-merge detection, unused-tag removal and smaller test plumbing remove further duplication. Two fresh full-file/diff passes covered all 37 changed paths; the second found no viable safe cuts. Runtime code lost 81 net lines, but meaningful regression coverage increased overall PR insertions/churn. The [detailed reduction record](../../evidence/source-metadata-2026-09-04/README.md#reduction-record) preserves exact metrics, skipped cuts and coverage receipts. The temporary plan was removed and the intermediate worktree was clean before second clean-pr.

The [post-reduction receipt](../../evidence/source-metadata-2026-09-04/post-reduction.json) records **3,225 tests passed, zero failures, 54 existing optional skips**, TypeScript, zero-warning full lint, a fresh normal production build and **nine actual-app browser workflows in 46.5 seconds**. Source/application fingerprints `9054411f36f270fb3c329df163b9538df1632a41ac3b670934352c25e24846ef` / `2131c232d2f75b669ed31fa34cfefa2a4a571f7346736ee18c81b70dccbc67c4` matched the final code and remained unchanged. The browser now verifies the initial focus pixels, all 24 exact source hashes, physical ordering, settings, labels, hard marks, reload and fresh-context ZIP restoration. Its preservation baseline follows an intentional navigation to slice 12; metadata preparation preserves progress 11/23. Complete restoration deliberately rotates the saved-work token.

Five current captures were individually inspected at original detail. The three earlier published images are byte-identical, and a new focus-preview PNG shows the formerly blank surface. Static scope remains explicit; other workflow images are retained without a fresh visual verdict. The 27-receipt resource sweep is closed; private MRI/weights, source, marks, raw failures, ZIPs, captures and authored harnesses remain protected. Second clean-pr refreshes reviewer evidence with this exact state; current-head CI, review and merge clearance remain owned by the live PR. This batch does not close F6/F7/F9/F11 or the larger audit goal.

### F6 durable-identity continuation: September 4

The preceding goal turn made concrete progress: PR #14 completed both clean-pr passes, two reduction passes and current-head Hall Monitor clearance, then merged as `b917f519db18e5ecc8c6c54d77e0a2ec047c2c01`. Its code, checks and original evidence remain retained. The full-audit objective continues from that merge on `blader/siqi-chen/audit-durable-identity`; no old browser/build run is restarted merely because the branch changed.

**Required outcome:** source-sensitive saved work remains attached to the actual Study/Series UIDs through patient regrouping and selective backup/restore. Unambiguous legacy work is recoverable; ambiguous settings and unverifiable label geometry remain preserved and discoverable without silently assigning an owner or inventing registration. Current dataset/token, source, transform, grid, hard-mark and stale-writer protections remain required. F7 warm-path measurements, F9 startup/host parity and F11 CI/release remain separate unfinished requirements.

**Architecture checkpoint:** live reads resolve saved labels through their owning study, but selection discovery still filters the stored patient-group string before consulting that authority. Volume keys also embed the dataset revision and reconstruction fingerprint, so removing a patient-string comparison alone would not establish complete reopen/restore recovery. In parallel, legacy panel hydration decides ambiguity from the current catalog, while selective export independently filters timestamp settings. A subset can lose the evidence that an assignment was ambiguous. These are static execution-path findings; the next step is production-module reproductions, not a completion claim.

An eager database-open rewrite would add migration ownership, scan every saved label and risk assigning unknown old geometry. Instead, keep recovery with the existing source/settings owners: verify the actual study and source, retain old records, share legacy candidate policy between reading and export, and preserve known ambiguity across a subset. Distinguish operation epochs from durable source evidence; do not simply relax revision or registration checks. The smallest proof is a grouped-patient discovery case and an actual ZIP subset round trip, followed by exact-source reopen/edit/restore coverage and a synthetic built-app workflow. Reopen this design if it needs guessed ownership, discards an original, weakens current source guards, or turns a read into a whole-database rewrite.

### F6 legacy-candidate checkpoint

Four production-module regressions reproduced the gaps at the merged application fingerprint: discovery returned zero retained grids after a conservative patient split, failed to find a split-key record after subset restoration, and ZIP subset restoration silently projected previously ambiguous settings. The original failure report is `frontend/tmp/durable-identity-before-01.json`; all four failures and their source receipt remain preserved.

`db/panelSettings.ts` now owns one legacy candidate/automatic-assignment decision used by both hydration and export. An optional per-date `assignmentRequired` field retains known unresolved ownership when a backup omits other candidates; it does not copy those candidates' identifiers into the subset. Re-export cannot erase that evidence. Unambiguous legacy settings still project without a write, and the existing explicit assignment action owns resolution. Malformed ambiguity metadata cannot authorize automatic assignment. Export leaves the original settings row unchanged.

Saved-grid discovery and explicit draft transfer now accept the historical grouping aliases of the actual validated study. They no longer hide otherwise compatible records merely because the conservative grouping key changed. Another study's alias remains rejected. The current dataset revision, study/source set, frame, geometry, source registration, support and hard-mark checks remain in force; a record from another revision is visible as retained, not silently transferred. No original label record is rewritten or deleted by discovery or transfer.

The final focused run passed **179 tests, zero failures and zero skips** across `localApi`, `exportBackup` and `svrSelectionMigration`. TypeScript project references, targeted zero-warning ESLint and formatting passed. Source/application fingerprints were `292d073f3094abb6a425276bfa01160a29325c9fdac25a097704e6a1c87b9450` / `115e2ae5389fce2f099961231d5d81ed8bbc3fa1758c614f63e349bd7fbd6b7b`, unchanged during those checks. These are synthetic Node/IndexedDB-emulator and physical ZIP round trips, not a new browser or full-suite acceptance claim. Logs/source receipts use the `frontend/tmp/durable-identity-` prefix.

**F6 is not complete.** Exact-grid reopening still addresses labels with a key containing the patient grouping and global dataset revision; native reconstruction fingerprints also contain that revision. A real restore rotates the dataset state, so making discovery visible does not prove automatic recovery of the exact original reviewed grid. The next proof must reopen/edit/restore actual source-bound labels across an unrelated import and a fresh-context backup without losing review state, marks or ownership. Removing epoch checks indiscriminately or treating an arbitrary grid copy as exact restoration would not satisfy that requirement. Keep operation cancellation/writer tokens separate from any proposed durable source/grid identity, preserve old records during migration, and verify equivalence before publishing a canonical copy. The whole-suite and built-app acceptance wait for this identity boundary to settle; F7/F9/F11 and the larger goal remain open.

### F6 exact-grid identity checkpoint

The production native constructor reproduced the epoch coupling: identical pixels, support and grid changed from `native-v1-702a1fb5` to `native-v1-924ab03e` after only a global dataset-revision increment. The failed receipt is `frontend/tmp/durable-grid-before-01.json`; the unchanged application/source snapshots are retained. This is a structural identity mismatch, not a failed registration or a reason to retry reconstruction.

The selected design separates two existing facts: accepted DICOM source/physical-grid identity addresses durable labels, while current dataset revision and saved-work tokens fence active work and writes. Stable keys must still change for different SOP inputs, source sets, transforms, output geometry or reconstruction settings. DICOM SOP identity remains the existing source authority; the new key is not a cryptographic pixel-content attestation. No new whole-acquisition hash scan or identity registry is proposed.

An eager migration would rewrite saved data on open, and an unguarded alias lookup could silently choose competing records. Instead, the existing readonly storage snapshot may project one uniquely verified, exact-grid legacy record. Older native fingerprints can be checked against their original revision-dependent formula; unknown geometry and multiple equivalent records do not grant automatic recovery. The first intentional edit must create the canonical record atomically while checking both the target revision and legacy source revision, retaining the original. An explicit empty checkpoint must prevent a later Clear action from resurrecting the retained legacy record.

The falsifiers are concrete: an unchanged-source epoch change still loses the reviewed grid; a real source or geometry replacement inherits it; discovery writes the database; a stale legacy writer is accepted; Clear resurrects old marks; or migration introduces unbounded payload scans. The next proof covers those cases at the constructor/storage/component boundaries, then in the actual synthetic browser workflow. Ordinary cross-grid transfer remains a separately reviewed draft operation, not a substitute for exact restoration.

### F6 implementation and browser checkpoint

Stable native fingerprints and source/grid selection keys now omit the global import revision and patient-group string. Accepted source provenance still carries the live revision; cancellation, study ownership, saved-work tokens and optimistic writer revisions remain checked. A readonly by-study lookup projects only one exact, verified historical grid. Native-v1 compatibility checks use the original revision-dependent formula, with a captured pre-change fingerprint as the independent regression oracle. No image pixels are rehashed or source records rewritten.

The first edit copies recovered work into the canonical key while checking both records in one transaction. It retains the original and captures the full checkpoint even when that edit has a sparse patch. Retry retains the original-record guard until the first copy commits; later edits use the canonical revision. Clear writes an empty checkpoint, so reopening cannot revive the retained original. Metadata-only lookup does not materialize unrelated chunk payloads.

The focused constructor/storage/viewer checks passed after making new typed-array assertions representation-independent across the test database's two realms. The two failed assertion runs remain preserved; no production storage logic changed to accommodate them. The full unit run passed **3,254 tests, zero failures and 54 existing skips** at source/application fingerprints `f89c153fadf3e86f0caa650a0d59a65d42aea14a56a54b9aee183d53f9ffb800` / `198bfb1734151c2db23b3faf59ebc6f7168ed66445503c2e7084494ffa348460`. Its serial wrapper then refused to overwrite an older lint log; the unit result remains valid, and lint runs separately under a fresh receipt name.

The normal-production browser case passed on headed Chrome 152.0.7977.82. A synthetic 36 × 36 × 24 source retained its reviewed 13-voxel selection, 13 Add marks and 13 Remove marks when a historical native key was reopened after an unrelated import changed the revision from 24 to 48. An intentional edit saved 26 selected voxels at the stable key, retaining the original. A real ZIP restored both records at revision 49 in a fresh context; image hashes, reviewed state and literal marks matched. Clear and reload left the canonical selection empty while the legacy original stayed intact. Three current captures were individually inspected at original detail; their verdict is static and limited to the visible area, not anatomical or motion acceptance. The 28-receipt scoped resource audit is closed.

The latest user request is to finish TurboVac and merge this branch without PR splitting. The first clean-pr pass is in progress; the reduction pass, second clean-pr, final-head Hall Monitor and merge still require observed completion. The subsequent browser-fixture-only lint correction does not change the application fingerprint; broader browser validation and final publication evidence will name their own source snapshots. F7/F9/F11 and the larger audit goal remain open; they are not silently included in this merge's completion claim.

### F6 reduction and F11 CI continuation: September 4

The first clean-pr published PR #15 at `ae19639bf8b2231eea155a89a10488d35c971aa0`. Two complete reduction passes directly read every changed text file and diff hunk. The retained refactor, committed normally as `440ced0256b73b53315027170ae114f5b0139056`, shares saved-record scope checks and copied lookup metadata in their existing owners. It removes 20 net production lines. Full-PR non-comment additions fall from 1,111 to 1,107; non-comment churn rises from 1,292 to 1,304 because older duplicate guards are removed too. All 16 comment-only additions, originals and synthetic evidence remain preserved. The broader browser-helper extraction increased PR additions/churn and was withdrawn; its failed typecheck and source remain archived. The final pass found no further viable F6 cut, and the temporary plan was archived before removal.

The kept source passed 361 focused tests, TypeScript and zero-warning lint at source/application fingerprints `8d09a0d121c9cdd308fb92fae6af97d85954b8eabc6857130e367f8f828ef770` / `c6a8ce233f2285a51269ce1ac47ac1a64633ae73da21aaedf8e962c8ceb331e1`. The subsequent complete-suite attempt failed with `ENOSPC` while publishing its JSON report. The truncated report is retained, not counted as passing evidence. Local production/browser replay remains unrun at this reduction checkpoint; prior acceptance keeps its original fingerprint.

**Execution-boundary decision:** the full audit already requires a repository-owned CI/release gate (F11), but the checkout had no workflow and the local host could not reliably write a report. GitHub Actions is enabled for this repository. Add one serial, read-only-permission workflow using the existing tests and builder; extend that builder with a production option and move its existing model verification into the same build owner. This runs the normal app without acceptance probes on a fresh hosted runner, with bounded unit/browser concurrency, pinned public models, source identities and synthetic-only artifacts. It follows the [documented Playwright CI setup](https://playwright.dev/docs/ci-intro); it does not change protection or require private fixtures. Preserve the earlier local failures and complete PR #15's current-head verification from the actual CI outcome, not from the workflow definition.

F11 is not yet declared complete. The new workflow must actually finish installation, lint, full units, production build and all ten browser workflows; inspect its raw results and durable saved-state receipts. New CI scope also needs publication/reduction review before final Hall Monitor clearance and merge. F7 still needs measured warm-navigation/canceled-work evidence; F9 still needs the unused tools-stack/dialog delivery changes and supported-host acceptance. Read-only tracing confirms those owners remain in the current source; neither finding is closed by adding CI.

### F11 runtime triage and post-reduction local acceptance

The expanded-scope first clean-pr, complete third reduction pass and second clean-pr finished at `5f5f4d3`; Hall Monitor then observed a real failure in [run 33870571156](https://github.com/blader/MiraViewer/actions/runs/33870571156). Installation, lint, 3,254 unit tests (54 existing optional skips) and the production build passed. Seven browser workflows passed; three 3D workflows timed out. Raw reports, traces and the original failed local ENOSPC report remain preserved. The host now has free space; no task artifacts were deleted to obtain it.

The unchanged production bundle reproduces the failure with the separate headless shell on SwiftShader. Regular Chromium headless on Metal passes the same focused workflow. Commit `736c24e` selects regular Chromium only for the workflow project; the hosted Linux rerun remains an independent gate. Its purpose is regular-browser parity, not a claim that software rendering is fast.

The full local replay then exposed a cancellation-test race: the synthetic model completed before the ordinary reconstruction click. The worker had retired normally, so expecting the previous draft was incorrect for that ordering. Reuse the existing inference-start observer to trigger the real reconstruction button while the operation is still current. The focused traced replay proves termination before completion; the uninstrumented full replay passes **all ten workflows in 42.0 seconds**, with exact saved labels, literal marks and original DICOM hashes intact. No production cancellation logic or model workload changed, and no timeout/retry/assertion was weakened.

The [post-reduction report](../../evidence/durable-identity-2026-09-04/README.md#post-reduction-acceptance-and-browser-runtime-repair) records the exact build/source distinction, fresh captures, failed attempts and a closed 35-receipt resource audit. Hosted current-head CI, final Hall Monitor clearance and merge are not inferred from those local passes. F7/F9 remain open.

The second [hosted run](https://github.com/blader/MiraViewer/actions/runs/33872668207) repeats the same three 3D failures with regular Chromium; complete unit and build results still pass. Its source/application fingerprints match the local production bundle, and traces show GL readback stalls. The binary-only hypothesis is therefore insufficient. Keep the production renderer and its quality unchanged; change CI's execution boundary to headed Chromium under Xvfb with Mesa OpenGL, following the [Playwright Linux setup](https://playwright.dev/docs/ci#running-headed) and [ANGLE's OpenGL selection](https://github.com/google/angle/blob/main/doc/DebuggingTips.md). Record actual renderer metadata in the existing workflow's receipts. The next acceptance condition is all ten unchanged workflows on this host within the existing timeouts; a repeated stall must be diagnosed from that renderer and trace rather than another blind flag or timeout change.

The next [hosted attempt](https://github.com/blader/MiraViewer/actions/runs/33874751474) passed lint, full units and production type/build, but its new receipt showed no WebGL2 context. None of the application scenarios ran. Rather than another blind CI retry, a two-profile Linux x64 experiment used the same Chromium 151.0.7922.34 with Xvfb/Mesa and no application/private inputs. The original launch logged `WebGL2 blocklisted`; adding only `--ignore-gpu-blocklist` admitted llvmpipe, produced the expected pixel and reported zero GL errors. Chromium's [software-driver policy](https://chromium.googlesource.com/chromium/src/+/refs/heads/main/gpu/config/software_rendering_list.json) explains the rejection. Both browsers and the owned container closed. The CI-only configuration now includes that flag; the full hosted application gate remains required. Original run receipts, trace and before/after GPU status are preserved in the linked post-reduction report. F7/F9 and the larger audit remain open.

### F11 hosted graphics verified; custom-workflow ordering repaired

[Run 33876055400](https://github.com/blader/MiraViewer/actions/runs/33876055400) verifies the software-driver repair: lint, all 3,254 default units, production type/build and nine browser workflows passed. Actual Linux Mesa WebGL2, both 3D saved-selection workflows, exact source/marks/backup preservation and three individually inspected static captures are retained. The overall run failed only in the custom-model harness, which applied a 15-second action timeout to initialization that validly took about 27 seconds under the existing 30-second product deadline.

Reuse the product initialization limit and the actual worker inference-start signal. The normal workflow no longer needs an artificially slow graph to keep a worker alive for a later click: the small real graph drives success, Cancel and reconstruction replacement. Start uses ordinary keyboard activation; the two interruption buttons are programmatically invoked at inference start, with no trusted-pointer latency claim. The independent cancellation probe retains its slow graph. No production deadlines, provider behavior, image quality or saved-state assertion changed.

The complete custom workflow passed in a bounded, traced Linux x64 replay using the retained production bundle. Both interruption traces show termination before any completion message; all three workers close and the exact original draft survives reload. The [detailed checkpoint](../../evidence/durable-identity-2026-09-04/README.md#hosted-linux-and-custom-model-timing-checkpoint) preserves the 191.4-second total, the emulation/renderer limitations, raw hashes, failed diagnostic setup attempts and container closure. This is focused local qualification, not final-head hosted clearance. F7/F9 and the larger audit remain open.

### F11 merged; F9 startup-delivery continuation

The subsequent [run 33878900501](https://github.com/blader/MiraViewer/actions/runs/33878900501) passed all 3,254 default unit tests (54 existing optional skips) and all ten production-browser workflows. PR #15 merged as `7da6db818a339a27ac2f9ffec853944d58aff370`; its tree exactly matches the passing integration checkout. The [local closeout](../../../artifacts/turbovac/durable-identity-2026-09-04/merge-closeout.md) retains the original receipts and scope limits. This closes the repository-owned F11 gate, not F7/F9.

F9 starts from that clean merge on `blader/siqi-chen/audit-startup-delivery`. Preserve the existing image loader, offline codecs, interaction semantics, dialog focus isolation, saved work and provider policy. Remove the unused tools/math/Hammer slice, deliver dialogs on demand through the existing dialog owner, and add the same isolation headers to the tracked Vercel host. Loading and failed dialog delivery must remain dismissible; a late import must not reopen a closed dialog.

The first production-browser failure test disproved in-place module-import retry: Chromium retained the rejected import and sent no second network request. Remove that retry counter rather than introduce cache-busted module identities. The failed dialog remains dismissible without resetting the workspace and offers an explicit reload, with a warning to finish saving first. It never reloads automatically. The original failed trace remains in `candidate-dialogs-01`. A separate baseline timing harness initially used a nonexistent “Close” action after import; the established workflow uses “Done”. Correct only that harness selector, retain the failed run and keep the actual import/save assertions.

The baseline production entry is 2,664,701 raw / 814,004 gzip bytes. Its new, separately retained bundle and exact source/model identity are recorded in `artifacts/performance/audit-startup-delivery-2026-09-04/baseline-01-build.json`. Compare entry bytes, cold shell and first-image time with the same browser/fixture; verify pan, zoom, wheel, outlines, overlays, compressed decoding and runtime assets. Keep imaging initialization at its existing owner unless measurement justifies a larger first-workspace split. A smaller entry alone is not latency or codec acceptance. Hosted headers must be observed on the actual deployed revision; configuration-only tests do not close host parity. No private inputs or existing receipts are changed or discarded.

### F7 measured execution boundary

F9's local production comparison and eleven browser workflows passed; the local summary is `artifacts/performance/audit-startup-delivery-2026-09-04/f9-local-summary-02.json`. Its exact RLE/uncompressed pixels and three individually inspected static captures are retained. Actual public Vercel aliases lacked isolation headers at the baseline; hosted and offline runtime qualification remain open.

F7 now has actual 256² and 512² native-grid playback measurements, including physical synthetic DICOM inputs, exact source/build identities, worker transfers, completed outputs, displayed image IDs and long tasks. A separate instrumented build records the preparation/compute branches without changing tracked production sources. At 256², median slab preparation was 0.77 ms versus 12.8 ms of compute. A rolling source-slab cache is therefore not the justified first change. The initial 512² delivery attempt hit Playwright's 50 MiB inline-input limit before importing anything; the same large fixture now uses retained physical files through the ordinary file picker.

**Failed outcome and evidence:** baseline 512² playback at 32 slices/s canceled all 33 uncached reslices and discarded 123.75 MiB of transferred input. Retaining one successful runtime improved median worker completion from about 42 to 33 ms, but still canceled all uncached work at that rate. The paired uninstrumented receipts are `warm-candidate-03` and `warm-baseline-03` under the same evidence directory. The numerical worker and its inputs remain unchanged.

**Required invariants:** native resolution and exact pixels/support; current-source and request fences; immediate manual cancellation; no lost saved work; explicit runtime disposal. **Current flow:** the slice clock changes shared progress independently of completion; the alignment request owner cancels any now-superseded job. **Classification:** a structural mismatch between automatic demand and completed visible output, not expensive slab preparation alone.

**Decision:** keep the measured one-idle-runtime improvement, and refactor automatic playback to wait for the current visible alignment result before applying more accumulated clock steps. Coalesce elapsed steps at the existing navigator, without introducing a worker queue, a second pixel cache, or permission to publish stale results. Manual navigation and Pause remain available. Waiting for all background targets would incorrectly couple visible playback to offscreen work; only required presented targets may delay it.

**Forecast and falsifier:** uncached native planes must complete during 512²/32-slice playback, with substantially fewer canceled transfers and unchanged result hashes. The control still covers the requested elapsed range by skipping superseded positions, not by lowering image resolution. Abandon or reassess this boundary if completed planes still starve, background alignment stalls a ready visible pair, cancellation stops working, or pixel/source parity changes. Extend existing navigator tests and rerun the same physical-source browser workload; do not treat reduced worker starts alone as completion.

### F7/F9 local acceptance and deployment gate

The [detailed report](../../evidence/startup-delivery-2026-09-04/README.md) records the paired native-plane experiment, exact hashes, startup costs, actual runtime configuration and preserved failures. The combined candidate completes 22 uncached 512² planes during two seconds of 4× playback; the baseline completes zero. Canceled transfer drops from 123.75 to 3.75 MiB. All 97 shared pixel/support pairs match. Sampled whole-browser memory increases by about 106 MiB; no memory-reduction or universal frame-rate claim is made.

All eleven ordinary production workflows pass in 43.8 seconds, with a further 7.0-second RLE-based import/navigation/annotation/reopen/backup replay. Ten fresh startup contexts preserve exact decoded pixels; the combined entry is 23.2% smaller raw and 19.1% smaller gzip. Full lint and production/browser TypeScript checks pass. The complete unit attempt passed 3,258 tests and exposed one synchronous-dialog expectation; the focused repair waits for actual delivery and preserves the original retained-import checks. Final-head CI must establish complete-suite clearance.

Actual preview, canonical dev and canonical offline executions return the exact 13,824-label synthetic oracle with eight configured WASM threads and verified complete runtime/pipeline/model assets. The public baseline also completes but is confirmed unisolated and single-threaded. Candidate Vercel qualification, current-head TurboVac clearance and merge remain open. No comparison inputs, private files, prior receipts or other sessions were removed or interrupted; the temporary dev runner's one owned orphan was explicitly identified and closed, and a graceful-shutdown rerun completed normally.

### PR #16 CI repair and remaining acceptance reconciliation

The next hosted runs cleared the complete default unit suite (3,259 passed, 54 optional skips), lint, build and ten normal workflows, but the custom-model workflow twice exceeded its execution/capture deadline. Trace readback and a failing production-component regression isolated an unnecessary full 3D draw on status-only React updates. The viewer's enhancement wrapper changed identity despite unchanged visual inputs. Remove its redundant render ref; use the existing effect-event primitive to read current committed values, and invalidate only actual enhancement display changes. Keep all shader quality, source pixels, fixtures, viewport and deadlines.

The original custom workflow, with observation hooks only, makes 32 versus 18 full GL submissions before/after this change. Its cancellation PNG bytes and saved/reopened labels match exactly. The corrected normal production build then passes all eleven uninstrumented workflows in 41.6 seconds; 149 focused viewer/enhancement tests, full lint and TypeScript checks pass. [The detailed checkpoint](../../evidence/startup-delivery-2026-09-04/README.md#ci-follow-up-status-updates-must-not-redraw-unchanged-3d-pixels) preserves the failure history, fingerprints and visual-inspection recovery. This is a local pre-push checkpoint, not fresh hosted clearance.

The F10 acceptance gap is now covered by a real independent-2D acquisition import, native-pointer Add/Remove editing and exact reviewed save/reopen. All 13 selected voxels, 13 Add and 13 Remove marks survive; Auto-fill remains intentionally unavailable, with no inference worker. This uses retained `candidate-04`, not a mock or a relabeled run of the later fix. The [supplemental receipts](../../evidence/startup-delivery-2026-09-04/followup-acceptance.json) preserve complete source identities and saved/reopened records.

F2's retained-runtime acceptance was rechecked against the existing `artifacts/performance/audit-retained-runtime-2026-09-02/attempt-02/summary.json`: retained correction takes 3,035.9 ms versus 4,227.0 ms fresh, with zero versus four session initializations, three conditioning frames and exact mask parity. Sampled whole-browser high-water is 1,309.19 versus 1,425 MiB; this is not an isolated or instantaneous WASM peak. All five workers close. No private model experiment or repeated benchmark was needed. The seven explicitly obsolete F8 modules remain absent. Their earlier evidence is preserved at its recorded revision.

The only remaining release gates at this snapshot are the corrected pushed head's CI/review/merge clearance and actual public-host runtime qualification. They are recorded in the [local merge closeout](../../../artifacts/turbovac/startup-delivery-2026-09-04/merge-closeout.md), without rewriting these dated experiment results. Clinical/anatomical validation, universal hardware performance and the intentionally omitted PR-splitting recommendation are not claimed by this work.

The next hosted attempt passed the new render regression but exposed a separate asynchronous test boundary before browser execution: `findByRole` observes the Help dialog's DOM before its passive focus effect necessarily runs. The same focus assertion now waits for that actual state, keeping the default deadline and all inert/Escape/trigger checks. This test-only repair passes six focused cases and the complete local suite: **3,260 passed, zero failed, 54 existing optional skips**. Application/model fingerprints are unchanged, so the already completed production replays are reused at their original identity; the new pushed head still needs hosted clearance.
